table:
  name: otoswing
  dataset: "{kpi_dataset}"
  fqn: '{project}.{kpi_dataset}.otoswing'
  layer: kpi
  description: OTO (Order Take Out) swing trade KPIs measuring aggressive sweep performance where the system detects an opportunity
    in the underlying and routes orders to capture edge across multiple price levels. Each row represents one OTO sweep event.
    Contains routed_price, routed_size, swing_edge, avg_trade_price, and time-bucketed slippage with unique fired_at variants
    that measure PnL relative to the routed/fired price (separate from the average filled price). Partitioned by trade_date.
  business_context: >
    Mako's automated OTO swing takeouts only. These trades also appear in kpi.markettrade.
    Do NOT sum with markettrade or you will double-count.
  preferred_timestamps:
    primary: event_timestamp_ns
    fallback: []
    notes: "Use event_timestamp_ns for all time-range filters on KPI otoswing."
  partition_field: trade_date
  cluster_fields:
  - symbol
  - portfolio
  row_count_approx: 0
  columns:
  - name: symbol
    type: STRING
    description: Exchange-listed symbol or ticker identifying the traded instrument (e.g., OESX, ODAX). This is the primary
      grouping key for analyzing performance by product. In options trading, this represents the root symbol without strike/expiry
      detail. Also referred to as ticker, product, or instrument by traders.
    synonyms:
    - ticker
    - product
    - instrument
    - root symbol
    source: 'SwingData.proto :: VtCommon.props'
  - name: portfolio
    type: STRING
    description: Trading portfolio or book to which this OTO swing trade is assigned. Portfolios group related trading activity
      for risk management and P&L attribution. Traders often call this the book or strategy. Commonly used as a filter to
      isolate performance by trading desk or strategy.
    synonyms:
    - book
    - trading book
    - strategy
    - desk
    source: 'SwingData.proto :: VtCommon.props'
  - name: algo
    type: STRING
    description: Algorithm or strategy identifier that generated this OTO swing trade. Identifies which trading algorithm
      or configuration was active when the swing was triggered. Useful for comparing performance across different algo versions
      or parameterizations.
    synonyms:
    - strategy
    - algo name
    - trading algo
    - algorithm
    source: 'SwingData.proto :: VtCommon.props'
  - name: ref_curve_id
    type: INTEGER
    description: Identifier of the volatility curve used at the reference snapshot for this OTO swing trade's KPI computation.
      Links to the specific vol surface parameterization active at trade time.
    synonyms:
    - ref curve
    - reference curve
  - name: ref_carry_id
    type: INTEGER
    description: Identifier of the carry/dividend curve used at the reference snapshot for this OTO swing trade's KPI computation.
      Links to the specific dividend and cost-of-carry assumptions active at trade time.
    synonyms:
    - ref carry
    - reference carry
  - name: ref_tv
    type: FLOAT
    description: Reference theoretical value (fair value) captured at the reference snapshot for KPI computation. This is
      the TV used as the baseline for slippage calculations. May differ slightly from the real-time tv field due to snapshot
      timing.
    synonyms:
    - reference theo
    - ref fair value
    - reference theoretical value
  - name: ref_base_price
    type: FLOAT
    description: Reference base (underlying) price captured at the reference snapshot for KPI computation. Used as the anchor
      point for delta-based slippage decomposition. Changes from this reference are what drive delta_slippage values.
    synonyms:
    - ref underlying
    - reference price
    - ref spot
  - name: ref_roll
    type: FLOAT
    description: Reference roll value captured at the KPI reference snapshot. Roll represents the cost-of-carry adjustment
      between forward and spot prices. Used in roll_slippage decomposition to isolate the impact of roll changes on PnL.
    synonyms:
    - reference roll
    - ref carry adjustment
  - name: raw_bv_bid
    type: FLOAT
    description: Raw base value (underlying) bid price before any layering or adjustment. The unadjusted bid price from the
      market data feed for the underlying instrument at the time of the OTO swing event.
    synonyms:
    - bv bid
    - base bid
    - underlying bid
  - name: raw_bv_bid_size
    type: INTEGER
    description: Raw base value (underlying) bid size at the time of the OTO swing event. The number of contracts available
      at the raw bid price, before any layering adjustments.
    synonyms:
    - bv bid size
    - base bid size
  - name: raw_bv_ask
    type: FLOAT
    description: Raw base value (underlying) ask price before any layering or adjustment. The unadjusted ask price from the
      market data feed for the underlying instrument at the time of the OTO swing event.
    synonyms:
    - bv ask
    - base ask
    - underlying ask
  - name: raw_bv_ask_size
    type: INTEGER
    description: Raw base value (underlying) ask size at the time of the OTO swing event. The number of contracts available
      at the raw ask price, before any layering adjustments.
    synonyms:
    - bv ask size
    - base ask size
  - name: layered_base_bid
    type: FLOAT
    description: Layered base bid price after the system applies adjustments for order flow, inventory, and market microstructure
      effects. This is the adjusted underlying bid used in pricing and edge calculations.
    synonyms:
    - adjusted base bid
    - layered bid
  - name: layered_base_bid_size
    type: INTEGER
    description: Size available at the layered (adjusted) base bid price. May differ from raw_bv_bid_size due to layering
      logic that accounts for order flow and inventory.
    synonyms:
    - adjusted bid size
  - name: layered_base_ask
    type: FLOAT
    description: Layered base ask price after the system applies adjustments for order flow, inventory, and market microstructure
      effects. This is the adjusted underlying ask used in pricing and edge calculations.
    synonyms:
    - adjusted base ask
    - layered ask
  - name: layered_base_ask_size
    type: INTEGER
    description: Size available at the layered (adjusted) base ask price. May differ from raw_bv_ask_size due to layering
      logic that accounts for order flow and inventory.
    synonyms:
    - adjusted ask size
  - name: layered_base_adjustment
    type: FLOAT
    description: The total adjustment applied to the raw base value to arrive at the layered base price. Captures the combined
      effect of all systematic adjustments (inventory, flow, etc.).
    synonyms:
    - base layer adj
    - layering offset
  - name: odr_adjustment
    type: FLOAT
    description: Order-driven adjustment applied to the base value pricing. This adjustment captures systematic effects of
      outstanding orders on the fair value estimate.
    synonyms:
    - order adjustment
    - odr adj
  - name: inflight_deflection_tv_adjustment_bid
    type: FLOAT
    description: TV adjustment on the bid side for inflight deflection effects. Captures the impact of orders currently in
      flight (sent but not yet confirmed) on the theoretical value.
    synonyms:
    - inflight adj bid
    - deflection bid
  - name: inflight_deflection_tv_adjustment_ask
    type: FLOAT
    description: TV adjustment on the ask side for inflight deflection effects. Captures the impact of orders currently in
      flight (sent but not yet confirmed) on the theoretical value.
    synonyms:
    - inflight adj ask
    - deflection ask
  - name: internal_tv_adjustment_bid
    type: FLOAT
    description: Internal TV adjustment on the bid side, capturing proprietary model corrections or overrides applied to the
      theoretical value before edge computation.
    synonyms:
    - internal adj bid
  - name: internal_tv_adjustment_ask
    type: FLOAT
    description: Internal TV adjustment on the ask side, capturing proprietary model corrections or overrides applied to the
      theoretical value before edge computation.
    synonyms:
    - internal adj ask
  - name: nhr_adjustment_bid
    type: FLOAT
    description: Non-hedgeable risk (NHR) adjustment applied to the bid side of the theoretical value. Accounts for costs
      and risks that cannot be hedged away, such as pin risk, discrete hedging costs, or model uncertainty. Subtracted from
      TV in edge calculations.
    synonyms:
    - nhr bid
    - nhr adj bid
    business_rules: 'Used in instant_edge_fired_at formula: edge = TV - nhr_adjustment_bid - price.'
  - name: nhr_adjustment_ask
    type: FLOAT
    description: Non-hedgeable risk (NHR) adjustment applied to the ask side of the theoretical value. Accounts for costs
      and risks that cannot be hedged away. Added to TV for ask-side edge calculations.
    synonyms:
    - nhr ask
    - nhr adj ask
  - name: base_edge_bid
    type: FLOAT
    description: Base edge on the bid side before any NHR or internal adjustments. Represents the raw theoretical profit if
      buying at the current bid, using the unadjusted TV.
    synonyms:
    - bid edge
    - raw bid edge
  - name: base_edge_ask
    type: FLOAT
    description: Base edge on the ask side before any NHR or internal adjustments. Represents the raw theoretical profit if
      selling at the current ask, using the unadjusted TV.
    synonyms:
    - ask edge
    - raw ask edge
  - name: fees
    type: FLOAT
    description: Total trading fees for this OTO swing trade, including exchange fees and clearing fees, in currency units.
      Deducted from PnL to arrive at net profitability. Always positive (a cost). Also known as exchange fees or transaction
      costs.
    synonyms:
    - exchange fees
    - transaction costs
    - trading costs
    - commissions
    business_rules: Always positive. Subtract from PnL for net calculations.
  - name: bv_side
    type: STRING
    description: Side of the base value update (bid or ask) that was most recently updated at the time of this OTO swing event.
      Indicates whether the triggering market data came from the bid or ask side of the underlying.
    synonyms:
    - base side
    - underlying side
    example_values:
    - BID
    - ASK
  - name: base_sequence_number
    type: STRING
    description: Sequence number of the base (underlying) market data event associated with this OTO swing trade. Used for
      precise event ordering and data integrity verification.
    synonyms:
    - base seq
  - name: option_sequence_number
    type: INTEGER
    description: Sequence number of the option market data event associated with this OTO swing trade. Used for precise event
      ordering and reconciliation.
    synonyms:
    - option seq
  - name: base_instrument_hash
    type: STRING
    description: Hash identifier for the base (underlying) instrument of this OTO swing trade. Used as a join key for underlying-level
      data lookups.
    synonyms:
    - base hash
    - underlying hash
  - name: hardware_nic_rx_timestamp
    type: TIMESTAMP
    description: Hardware NIC (network interface card) receive timestamp, recording when the triggering market data packet
      was received at the network hardware level. Used for ultra-precise latency measurement from market data receipt to order
      routing.
    synonyms:
    - nic timestamp
    - hw timestamp
    - hardware timestamp
  - name: hardware_nic_rx_timestamp_ns
    type: INTEGER
    description: Nanosecond-precision version of the hardware NIC receive timestamp, stored as a 64-bit integer. Provides
      the highest precision timing for latency measurement from market data receipt to order routing.
    synonyms:
    - nic ns
    - hw ns
  - name: takeout_category
    type: STRING
    description: High-level classification of what triggered this OTO swing opportunity. Categories include UnderMove (underlying
      price movement), OptionTick (option market change), TheoChange (theoretical value recalculation), and Invalid. Helps
      traders understand what type of market event the system is reacting to.
    synonyms:
    - swing trigger
    - opportunity type
    - takeout reason
    business_rules: Filter on takeout_category to analyze performance by trigger type. Invalid entries should be excluded
      from analysis.
    source: 'SwingData.proto :: SwingData.takeoutCategory'
    example_values:
    - UnderMove
    - OptionTick
    - TheoChange
    - Invalid
  - name: base_order_event_type
    type: STRING
    description: The type of the base (underlying) order event that triggered or is associated with this OTO swing. Classifies
      the underlying market activity (e.g., new order, cancel, fill) that the swing system responded to.
    synonyms:
    - base event type
    - underlying event
    source: 'SwingData.proto :: SwingData.baseOrderEventType'
  - name: base_order_event_side
    type: STRING
    description: The side (bid/ask or buy/sell) of the base (underlying) order event associated with this OTO swing. Indicates
      whether the triggering event was on the bid or ask side of the underlying market.
    synonyms:
    - base event side
    - underlying side
    source: 'SwingData.proto :: SwingData.baseOrderEventSide'
  - name: takeout_type
    type: STRING
    description: Finer-grained classification of the OTO swing takeout opportunity, providing more detail than takeout_category.
      Identifies the specific sub-type of market event that triggered the sweep decision. Useful for granular performance
      attribution and strategy tuning.
    synonyms:
    - swing subtype
    - detailed trigger
    source: 'SwingData.proto :: SwingData.takeoutType'
  - name: event_type
    type: STRING
    description: General event type classification for this OTO swing record. Indicates the nature of the event in the swing
      processing pipeline, used for filtering and categorization.
    synonyms:
    - swing event type
    source: 'SwingData.proto :: SwingData.eventType'
  - name: fanout_identifier
    type: INTEGER
    description: Identifier linking related OTO swing events that fanned out from a single opportunity. When one underlying
      move triggers sweeps across multiple option strikes, they share the same fanout_identifier. Used to group and analyze
      correlated sweep activity.
    synonyms:
    - fanout id
    - group id
    source: 'SwingData.proto :: SwingData.fanoutIdentifier'
  - name: fanout_count
    type: INTEGER
    description: Total number of OTO swing events that fanned out from the same underlying opportunity, sharing the same fanout_identifier.
      A count of 1 means this was a single-instrument sweep; higher values indicate the system swept multiple strikes simultaneously.
    synonyms:
    - num fanouts
    - fanout total
    source: 'SwingData.proto :: SwingData.fanoutCount'
  - name: opportunity_index
    type: INTEGER
    description: Zero-based index of this specific opportunity within a set of related OTO swing opportunities. Used in conjunction
      with num_opportunities to identify the position of this sweep attempt in a sequence of related opportunities.
    synonyms:
    - opp index
    source: 'SwingData.proto :: SwingData.opportunityIndex'
  - name: num_opportunities
    type: INTEGER
    description: Total number of opportunities detected in this OTO swing event group. Indicates how many potential sweep
      opportunities the system identified in this batch. Higher values suggest a more active sweep event.
    synonyms:
    - total opportunities
    - opp count
    source: 'SwingData.proto :: SwingData.numOpportunities'
  - name: swing_properties
    type: INTEGER
    description: Bitmask field encoding various boolean properties of this OTO swing trade. Each bit represents a different
      property flag (e.g., lean-back enabled, spark path, negative edge allowed). Used internally for detailed trade classification
      and filtering.
    synonyms:
    - swing flags
    - swing bitmask
    business_rules: Bitmask field -- individual bits encode different properties. Use bitwise AND to check specific flags.
    source: 'SwingData.proto :: SwingData.swingProperties'
  - name: market_size
    type: INTEGER
    description: The size (number of contracts) available at the market price at the time of the OTO swing event. Represents
      the visible liquidity on the relevant side of the book when the opportunity was detected. Used to assess whether the
      sweep was proportional to available liquidity. Also known as mkt size.
    synonyms:
    - mkt size
    - available size
    - book size
    - available liquidity
    source: 'SwingData.proto :: SwingData.marketSize'
  - name: routed_size
    type: FLOAT
    description: The number of contracts in the order as routed (fired) to the exchange, expressed as a float to handle fractional
      lot scenarios. Traders call this fire size. This is the intended order size, which may differ from trade_volume if partial
      fills occurred. Used as the multiplier for all fired_at PnL and slippage calculations.
    synonyms:
    - fire size
    - fired size
    - sent size
    - order size
    business_rules: Used as the quantity multiplier for all fired_at metrics. May differ from trade_volume on partial fills.
    source: 'SwingData.proto :: SwingData.routedSize'
  - name: avg_trade_price
    type: FLOAT
    description: Volume-weighted average execution price across all fills in this OTO sweep trade, expressed in the instrument's
      native price units. This is the actual average price at which the order was filled, which may differ from the routed_price
      if partial fills occurred at different levels. Also known as avg fill price or average execution price.
    synonyms:
    - avg fill price
    - average execution price
    - fill price
    - execution price
    business_rules: May differ from routed_price due to partial fills at multiple price levels in the sweep.
    source: 'SwingData.proto :: SwingData.avgTradePrice'
  - name: swing_edge
    type: FLOAT
    description: Raw edge captured by this OTO swing trade in ticks, representing the theoretical profit per unit at the moment
      of the sweep. This is the primary profitability metric for swing trades, measuring how much theoretical value was captured
      by the aggressive sweep. Positive means profitable. Traders refer to this as swing profit or swing PnL.
    synonyms:
    - swing profit
    - swing pnl
    - sweep edge
    - oto edge
    - swing capture
    business_rules: Raw edge before any adjustments or fees. Always check with instant_edge for the KPI-adjusted version.
    source: 'SwingData.proto :: SwingData.swingEdge'
  - name: trade_volume
    type: INTEGER
    description: Total number of contracts executed in this OTO sweep event, representing the aggregate volume across all
      fills. Expressed as an integer count of lots/contracts. Use abs_trade_volume for the unsigned version. Also referred
      to as fill volume or executed volume by traders.
    synonyms:
    - fill volume
    - executed volume
    - lots
    - quantity
    - size
    - trade size
    business_rules: 'Signed value: positive for buys, negative for sells. Use abs_trade_volume for unsigned aggregation.'
    source: 'SwingData.proto :: SwingData.tradeVolume'
  - name: mako_order_id
    type: INTEGER
    description: Internal Mako system order identifier for this OTO swing trade. Unique within the trading system and used
      to trace the order through the full lifecycle from routing to fill. Useful for joining with order management system
      data.
    synonyms:
    - order id
    - mako id
    source: 'SwingData.proto :: SwingData.makoOrderId'
  - name: exch_order_id
    type: STRING
    description: Exchange-assigned order identifier for this OTO swing trade. Assigned by the matching engine upon order receipt.
      Used for reconciliation with exchange trade confirmations and drop copies.
    synonyms:
    - exchange order id
    - exch id
    source: 'SwingData.proto :: SwingData.exchOrderId'
  - name: exch_trans_id
    type: INTEGER
    description: Exchange transaction identifier for the fill associated with this OTO swing trade. Assigned by the exchange
      matching engine at execution time. Used for trade reconciliation and regulatory reporting.
    synonyms:
    - exchange transaction id
    - transaction id
    source: 'SwingData.proto :: SwingData.exchTransId'
  - name: latency_stats_prcx
    type: INTEGER
    description: Latency statistic for the price-cross (option price crossing threshold) stage of the OTO swing processing
      pipeline, measured in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing.
      Used for performance monitoring and infrastructure optimization.
    synonyms:
    - prcx latency
    - latency prcx
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsPRCX'
  - name: latency_stats_prcs
    type: INTEGER
    description: Latency statistic for the price-cross send (order send after price cross) stage of the OTO swing processing
      pipeline, measured in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing.
      Used for performance monitoring and infrastructure optimization.
    synonyms:
    - prcs latency
    - latency prcs
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsPRCS'
  - name: latency_stats_vtio
    type: INTEGER
    description: Latency statistic for the VT IO (theoretical value IO processing) stage of the OTO swing processing pipeline,
      measured in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing. Used for
      performance monitoring and infrastructure optimization.
    synonyms:
    - vtio latency
    - latency vtio
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsVTIO'
  - name: latency_stats_bkio
    type: INTEGER
    description: Latency statistic for the book IO (order book processing) stage of the OTO swing processing pipeline, measured
      in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing. Used for performance
      monitoring and infrastructure optimization.
    synonyms:
    - bkio latency
    - latency bkio
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsBKIO'
  - name: latency_stats_qmio
    type: INTEGER
    description: Latency statistic for the quote manager IO (quote management processing) stage of the OTO swing processing
      pipeline, measured in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing.
      Used for performance monitoring and infrastructure optimization.
    synonyms:
    - qmio latency
    - latency qmio
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsQMIO'
  - name: latency_stats_itio
    type: INTEGER
    description: Latency statistic for the IT IO (internal transfer processing) stage of the OTO swing processing pipeline,
      measured in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing. Used for
      performance monitoring and infrastructure optimization.
    synonyms:
    - itio latency
    - latency itio
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsITIO'
  - name: latency_stats_itex
    type: INTEGER
    description: Latency statistic for the IT execution (internal transfer execution) stage of the OTO swing processing pipeline,
      measured in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing. Used for
      performance monitoring and infrastructure optimization.
    synonyms:
    - itex latency
    - latency itex
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsITEX'
  - name: exchange_timestamp
    type: TIMESTAMP
    description: Timestamp assigned by the exchange matching engine at the time of trade execution. Represents the authoritative
      execution time as recorded by the venue. Used for regulatory timestamping and execution quality analysis.
    synonyms:
    - exchange time
    - execution time
    - fill time
  - name: exchange_timestamp_ns
    type: INTEGER
    description: Nanosecond-precision exchange timestamp stored as a 64-bit integer. Provides the highest resolution execution
      time from the exchange matching engine.
    synonyms:
    - exchange time ns
  - name: mako_symbol
    type: STRING
    description: Internal Mako system symbol for the instrument, which may differ from the exchange symbol. Used for internal
      system lookups and cross-referencing with Mako's instrument registry.
    synonyms:
    - internal symbol
    - mako ticker
  - name: pid
    type: STRING
    description: Process identifier of the trading engine instance that generated this OTO swing event. Used for operational
      debugging and identifying which server processed the trade.
    synonyms:
    - process id
  - name: hostname
    type: STRING
    description: Hostname of the server that processed this OTO swing trade. Used for operational monitoring, debugging, and
      identifying which trading infrastructure handled the event.
    synonyms:
    - server
    - host
  - name: fanout_index
    type: INTEGER
    description: Zero-based index of this specific instrument within a fanout group of OTO swing events. When a single underlying
      move triggers sweeps across multiple strikes, each gets a unique fanout_index within the shared fanout_identifier group.
    synonyms:
    - fanout position
    source: SwingData.proto
  - name: bv_update_sequence_number
    type: INTEGER
    description: Sequence number of the base value update used for this OTO swing trade. Ensures correct ordering of underlying
      price updates.
    synonyms: []
  - name: bv_update_instrument_hash
    type: STRING
    description: Instrument hash of the base value update source. Identifies which specific underlying instrument provided
      the base value.
    synonyms: []
  - name: underlying_sequence_number
    type: INTEGER
    description: Sequence number of the underlying market data event. Used for precise ordering of underlying price updates.
    synonyms: []
  - name: underlying_instrument_hash
    type: STRING
    description: Hash identifier for the underlying instrument. Used as a join key for underlying-level data lookups.
    synonyms: []
  - name: underlying_exe_exch
    type: INTEGER
    description: Numeric exchange identifier for the underlying execution venue.
    synonyms: []
  - name: underlying_exe_exch_name
    type: STRING
    description: Human-readable name of the underlying execution exchange.
    synonyms: []
  - name: is_spark
    type: BOOLEAN
    description: Boolean indicating whether this OTO swing event used the Spark (fast-path) execution pipeline. Spark events
      have lower latency due to optimized processing.
    synonyms:
    - spark flag
    - fast path
    business_rules: Spark-path events typically have lower latency. Filter by is_spark for latency analysis.
  - name: ref_inst_carry_id
    type: INTEGER
    description: Reference instrument carry curve identifier, linking to the carry model used for this specific instrument
      at the reference snapshot.
    synonyms: []
  - name: ref_inst_curve_id
    type: INTEGER
    description: Reference instrument volatility curve identifier, linking to the vol surface used for this specific instrument
      at the reference snapshot.
    synonyms: []
  - name: min_edge
    type: FLOAT
    description: Minimum required edge threshold (in ticks) that must be met for the OTO swing system to trigger and route
      the order. This is the post-adjustment threshold. If the estimated edge is below this value, the sweep will not be fired.
      Key parameter for tuning swing aggressiveness. Also known as required edge.
    synonyms:
    - required edge
    - required_edge_bps
    - min required edge
    - edge threshold
    business_rules: Orders are only routed when estimated edge >= min_edge. Lower values = more aggressive swing strategy.
    source: 'SwingData.proto :: SwingData.minEdge (added 8a2fe67)'
  - name: raw_min_edge
    type: FLOAT
    description: Raw minimum edge threshold before any adjustments or overrides are applied. This is the base threshold from
      the configuration, before lean-back, edge model, or other dynamic adjustments modify it into min_edge. Added in commit
      8a2fe67.
    synonyms:
    - unadjusted min edge
    - base min edge
    business_rules: Compare raw_min_edge vs min_edge to see how much adjustment was applied.
    source: 'SwingData.proto :: SwingData.rawMinEdge (added 8a2fe67)'
  - name: is_ref_theo_invalid
    type: BOOLEAN
    description: Boolean flag indicating whether the reference theoretical value was marked as invalid at the time of the
      OTO swing trade. Invalid theo can make edge calculations unreliable.
    synonyms:
    - invalid theo
    - bad theo
    business_rules: Filter out rows where is_ref_theo_invalid = true for reliable edge analysis.
  - name: lb_enabled
    type: BOOLEAN
    description: Boolean indicating whether lean-back mode was enabled for this OTO swing trade. Lean-back mode widens edge
      thresholds to reduce aggressive trading during uncertain market conditions.
    synonyms:
    - lean back
    - leanback
    business_rules: When true, min_edge was widened. May indicate less favorable market conditions.
  - name: edge_model_type
    type: INTEGER
    description: Numeric identifier for the edge model type used to evaluate this OTO swing trade. Different edge models may
      weight factors differently. Use edge_model_type_name for the readable version.
    synonyms:
    - model type
    - edge model
  - name: edge_model_type_name
    type: STRING
    description: Human-readable name of the edge model type used for this OTO swing trade. Identifies which pricing/edge model
      configuration was active.
    synonyms:
    - model name
    - edge model name
  - name: is_negative_edge_allowed
    type: BOOLEAN
    description: Boolean flag indicating whether this OTO swing was allowed to trade with negative edge (i.e., at a theoretical
      loss). When true, the system may route orders even when estimated edge is negative, typically for hedging or risk-reduction
      purposes. Added in git history.
    synonyms:
    - neg edge allowed
    business_rules: When true, the swing system may intentionally take negative-edge trades. Filter these out for pure alpha
      analysis.
    source: 'SwingData.proto :: SwingData.isNegativeEdgeAllowed'
  - name: latency_stats_bvio
    type: INTEGER
    description: Latency statistic for the base value IO (underlying value processing) stage of the OTO swing processing pipeline,
      measured in nanoseconds. Part of the end-to-end latency breakdown from market data receipt to order routing. Used for
      performance monitoring and infrastructure optimization.
    synonyms:
    - bvio latency
    - latency bvio
    business_rules: Value in nanoseconds. Divide by 1000 for microseconds, by 1000000 for milliseconds.
    source: 'SwingData.proto :: SwingData.latencyStatsBVIO'
  - name: ref_theoevent_stream_id
    type: INTEGER
    description: Stream identifier of the reference theo event, used for data provenance and tracing which pricing stream
      produced the reference theoretical value.
    synonyms: []
  - name: ref_theoevent_event_id
    type: INTEGER
    description: Event identifier of the reference theo event, used for precise identification of which pricing event produced
      the reference theoretical value.
    synonyms: []
  - name: base_size
    type: FLOAT
    description: Size of the base (underlying) component associated with this OTO swing trade. Relevant for delta-hedging
      calculations and understanding the underlying exposure of the sweep. Added in commit 8e7a613.
    synonyms:
    - underlying size
    source: 'SwingData.proto :: SwingData.baseSize (added 8e7a613)'
  - name: routed_size_capped
    type: FLOAT
    description: The routed size after applying any capping or position limits. May be smaller than routed_size if risk limits
      or position constraints were active at the time of routing. Represents the effective order size after risk controls.
    synonyms:
    - capped size
    - limited size
    business_rules: Will equal routed_size when no caps are active. Check for differences to identify risk-limited orders.
    source: kpi_otoswing_normalized.sql
  - name: market_price
    type: FLOAT
    description: Market price of the option at the time of the OTO swing event, representing the best available price on the
      relevant side of the book at the moment the opportunity was detected. Expressed in the instrument's native price units.
      Used to assess the market conditions at the time of the swing decision.
    synonyms:
    - mkt price
    - market level
    - exchange price
    source: 'SwingData.proto :: SwingData.marketPrice'
  - name: routed_price
    type: FLOAT
    description: The price at which the OTO sweep order was initially routed (fired) to the exchange, expressed in the instrument's
      native price units. This is the price the system decided to send, which may differ from avg_trade_price if the order
      sweeps multiple levels. Traders call this the fire price. Key for fired_at PnL calculations.
    synonyms:
    - fire price
    - fired price
    - sent price
    - routed at
    business_rules: This is the decision price, not the fill price. Compare with avg_trade_price to measure execution quality.
    source: 'SwingData.proto :: SwingData.routedPrice'
  - name: trade_row_key
    type: STRING
    description: Unique key identifying this specific trade row. Used as the primary key for deduplication and joins. Guaranteed
      unique within the table.
    synonyms:
    - trade key
    - row id
    - primary key
    business_rules: Use as the unique row identifier for joins and deduplication.
  - name: trade_type
    type: STRING
    description: Classification of the trade type within the KPI system. For this table, will typically indicate an OTO/swing
      classification.
    synonyms:
    - type
    - trade classification
  - name: trade_date
    type: DATE
    description: Business date of the OTO swing trade, formatted as DATE. This is the partition column -- ALWAYS include a
      filter on trade_date in queries for performance. Represents the trading session date (not calendar date). Also known
      as date, business date, or trading date.
    synonyms:
    - date
    - business date
    - trading date
    - session date
    business_rules: PARTITION COLUMN -- always filter on trade_date to avoid full table scans. Use DATE type comparisons.
  - name: parent_is_combo
    type: BOOLEAN
    description: Boolean indicating whether the parent instrument is a combination (combo) or spread trade, such as a straddle,
      strangle, or calendar spread. When true, this row may be a leg of a multi-leg structure.
    synonyms:
    - is combo
    - is spread
    - combo flag
  - name: is_parent
    type: BOOLEAN
    description: Boolean indicating whether this row represents the parent (combo-level) record rather than an individual
      leg. When true, the KPIs represent the aggregate combo; when false, this is a specific leg.
    synonyms:
    - combo level
    - parent row
    business_rules: Filter is_parent = true for combo-level analysis, false for leg-level analysis.
  - name: is_auction
    type: BOOLEAN
    description: Boolean indicating whether this OTO swing trade occurred during an exchange auction period (e.g., opening/closing
      auction). Auction dynamics differ significantly from continuous trading.
    synonyms:
    - auction trade
    - in auction
    business_rules: Consider filtering out auction trades for intraday performance analysis.
  - name: contract_size
    type: FLOAT
    description: Contract multiplier for the traded instrument, representing the notional value per point of price movement.
      For example, 100 for standard equity options (100 shares per contract). Used in all PnL calculations to convert per-unit
      edge to currency. Also known as multiplier or point value.
    synonyms:
    - multiplier
    - point value
    - contract multiplier
    - lot size
  - name: instrument_hash
    type: STRING
    description: Unique hash identifier for the specific option instrument (including strike and expiry). Used as a join key
      to link with other tables containing instrument-level data. Provides a stable identifier across systems for the exact
      contract traded.
    synonyms:
    - instrument id
    - instrument key
    - inst hash
  - name: parent_instrument_hash
    type: STRING
    description: Instrument hash of the parent combo for this leg. Null or empty for non-combo trades. Use to join legs to
      their parent combo record.
    synonyms:
    - parent hash
    - combo hash
  - name: parent_feed_code
    type: STRING
    description: Feed code (exchange identifier) of the parent combo instrument. Used for exchange reconciliation of multi-leg
      structures.
    synonyms:
    - parent code
  - name: no_legs
    type: INTEGER
    description: Number of legs in the combo structure. 1 for single instruments, 2 for spreads, 3+ for butterflies and other
      multi-leg strategies.
    synonyms:
    - num legs
    - leg count
    - number of legs
  - name: leg_index
    type: INTEGER
    description: Zero-based index identifying this specific leg within the combo structure. Combined with parent_instrument_hash,
      uniquely identifies each leg.
    synonyms:
    - leg number
    - leg position
  - name: leg_ratio
    type: STRING
    description: Ratio of this leg in the combo, typically expressed as a string like '1' or '-1' indicating direction and
      multiplier within the spread structure.
    synonyms:
    - leg multiplier
    - leg weight
  - name: strike
    type: FLOAT
    description: Option strike price (exercise price) for this OTO swing trade. The price at which the option holder can exercise.
      Fundamental to moneyness calculations. Also known as K in pricing formulas.
    synonyms:
    - exercise price
    - strike price
    - K
    - exercise level
  - name: option_type
    type: INTEGER
    description: Option type code indicating whether this is a call or put option. Encoded as an integer (typically 0=call,
      1=put or exchange-specific). Use option_type_name for the human-readable version.
    synonyms:
    - call put
    - C/P
    - option type code
    business_rules: Use option_type_name for readable filtering. Numeric encoding is exchange-specific.
  - name: currency_id
    type: INTEGER
    description: Currency identifier for the instrument's denomination. Maps to the currency in which PnL and fees are expressed.
    synonyms:
    - ccy
    - currency code
  - name: parent_combo_type_name
    type: STRING
    description: Combo type name of the parent instrument (e.g., Straddle, Strangle, Spread). Describes the structure of the
      multi-leg trade.
    synonyms:
    - combo type
    - spread type
  - name: option_type_name
    type: STRING
    description: 'Human-readable option type name: ''Call'' or ''Put''. Use this field for filtering and grouping by option
      type instead of the numeric option_type code.'
    synonyms:
    - call put name
    - option kind
    example_values:
    - Call
    - Put
  - name: underlying_inst_type_name
    type: STRING
    description: Type name of the underlying instrument (e.g., Future, Equity Index). Describes the asset class of the underlying.
    synonyms:
    - underlying type
  - name: inst_type_name
    type: STRING
    description: Instrument type name for the traded product (e.g., Option, Future, Spread). Useful for filtering by product
      type.
    synonyms:
    - instrument type
    - product type
  - name: term
    type: STRING
    description: Expiry term or tenor label for the option contract (e.g., monthly, weekly, quarterly). Useful for grouping
      by expiry frequency and analyzing performance by tenor.
    synonyms:
    - tenor
    - expiry term
    - maturity
    - expiry cycle
  - name: expiry_timestamp
    type: TIMESTAMP
    description: Expiration timestamp of the option contract. The exact date and time at which the option ceases to exist.
      Used for time-to-expiry calculations and identifying expiry-day trades.
    synonyms:
    - expiry
    - expiration
    - maturity date
    - expiry date
  - name: is_bv_invalid
    type: STRING
    description: Flag indicating whether the base value (underlying price) was considered invalid at the time of the OTO swing
      trade. Invalid base values can lead to unreliable edge calculations.
    synonyms:
    - invalid bv
    - bv quality
    business_rules: Filter out rows where is_bv_invalid = true for reliable analysis.
  - name: option_exchange
    type: STRING
    description: Exchange where the option leg is listed. Identifies the specific venue for the option side of the trade.
    synonyms:
    - opt exchange
    - option venue
  - name: underlying_exchange
    type: STRING
    description: Exchange where the underlying instrument is traded. Identifies the venue for the underlying side used in
      hedging and base value calculations.
    synonyms:
    - ul exchange
    - underlying venue
  - name: streamsource_stream_id
    type: INTEGER
    description: 'Data provenance field: streamsource_stream_id. Used to trace this record back to the original data stream
      for debugging and audit purposes.'
    synonyms: []
  - name: streamsource_event_id
    type: INTEGER
    description: 'Data provenance field: streamsource_event_id. Used to trace this record back to the original data stream
      for debugging and audit purposes.'
    synonyms: []
  - name: streamsource_message_id
    type: INTEGER
    description: 'Data provenance field: streamsource_message_id. Used to trace this record back to the original data stream
      for debugging and audit purposes.'
    synonyms: []
  - name: datasource_pid
    type: INTEGER
    description: Process ID of the data source that produced this record. Used for provenance tracking and operational debugging.
    synonyms: []
  - name: datasource_hostname
    type: STRING
    description: Hostname of the data source server that produced this record. Used for provenance tracking and operational
      monitoring.
    synonyms: []
  - name: ref_delta
    type: FLOAT
    description: Reference delta used in the KPI computation pipeline, captured at the reference snapshot time. May differ
      slightly from the real-time delta due to timing. Used in slippage decomposition formulas to attribute PnL changes to
      delta, vol, and other components.
    synonyms:
    - reference delta
    - kpi delta
    business_rules: Used as the delta input for all slippage decomposition formulas.
    source: kpi_otoswing_normalized.sql
  - name: ref_gamma
    type: FLOAT
    description: Reference gamma used in the KPI computation pipeline. Captured at the reference snapshot and used in the
      second-order Taylor expansion for swing_mid_tv and in slippage decomposition. Measures convexity of the option position.
    synonyms:
    - reference gamma
    - kpi gamma
    business_rules: Used in swing_mid_tv formula and delta_slippage second-order corrections.
    source: kpi_otoswing_normalized.sql
  - name: mdepth_sequence_number
    type: INTEGER
    description: Market depth snapshot sequence number at the time of the OTO swing trade. Used for precise event ordering
      of market data snapshots.
    synonyms: []
  - name: curve_id
    type: INTEGER
    description: Volatility curve identifier at the time of the OTO swing trade. Links to the specific vol surface parameterization
      used for pricing. Changes in curve_id between intervals indicate vol curve recalibrations.
    synonyms:
    - vol curve
    - curve identifier
  - name: carry_id
    type: INTEGER
    description: Carry/dividend curve identifier at the time of the OTO swing trade. Links to the specific cost-of-carry model
      used for forward pricing.
    synonyms:
    - dividend curve
    - carry curve
  - name: is_mdepth_snapshot
    type: BOOLEAN
    description: Boolean indicating whether the market depth data used was from a periodic snapshot (true) or a real-time
      incremental update (false). Snapshots may have slightly stale data.
    synonyms: []
  - name: is_theodata_snapshot
    type: BOOLEAN
    description: Boolean indicating whether the theoretical data (TV, greeks) used was from a periodic snapshot rather than
      a real-time update. Snapshots may have marginally different values.
    synonyms: []
  - name: tv_offset
    type: FLOAT
    description: Offset applied to the theoretical value at the time of the OTO swing trade. Captures any manual or systematic
      adjustments to the base TV.
    synonyms:
    - theo offset
    - tv adj
  - name: event_timestamp_ns
    type: INTEGER
    description: Nanosecond-precision timestamp of the OTO swing event, stored as a 64-bit integer (epoch nanoseconds). Provides
      higher resolution than event_timestamp for latency analysis and precise event ordering.
    synonyms:
    - event time ns
    - nano timestamp
  - name: option_mdepth_timestamp
    type: TIMESTAMP
    description: 'Timestamp field: option mdepth timestamp. Records the time of the associated event in the OTO swing processing
      pipeline. Used for latency measurement and event ordering.'
    synonyms: []
  - name: option_mdepth_timestamp_ns
    type: INTEGER
    description: Nanosecond-precision version of option_mdepth_timestamp, stored as a 64-bit integer for ultra-precise timing
      and latency analysis.
    synonyms: []
  - name: theo_event_tx_timestamp
    type: TIMESTAMP
    description: 'Timestamp field: theo event tx timestamp. Records the time of the associated event in the OTO swing processing
      pipeline. Used for latency measurement and event ordering.'
    synonyms: []
  - name: theo_event_tx_timestamp_ns
    type: INTEGER
    description: Nanosecond-precision version of theo_event_tx_timestamp, stored as a 64-bit integer for ultra-precise timing
      and latency analysis.
    synonyms: []
  - name: when_timestamp
    type: TIMESTAMP
    description: 'Timestamp field: when timestamp. Records the time of the associated event in the OTO swing processing pipeline.
      Used for latency measurement and event ordering.'
    synonyms: []
  - name: when_timestamp_ns
    type: INTEGER
    description: Nanosecond-precision version of when_timestamp, stored as a 64-bit integer for ultra-precise timing and latency
      analysis.
    synonyms: []
  - name: bid_price_0
    type: FLOAT
    description: Best bid price (top of book) for the option at the time of the OTO swing trade. Level 0 represents the most
      aggressive bid. Used for spread analysis and execution quality assessment.
    synonyms:
    - bid 0
    - best bid
    - top bid
  - name: bid_volume_0
    type: INTEGER
    description: Volume at the best bid price (level 0) for the option at the time of the OTO swing trade. Indicates available
      liquidity on the bid side.
    synonyms:
    - bid size 0
    - best bid size
  - name: ask_price_0
    type: FLOAT
    description: Best ask price (top of book) for the option at the time of the OTO swing trade. Level 0 represents the most
      aggressive offer. Used for spread analysis and execution quality assessment.
    synonyms:
    - ask 0
    - best ask
    - top ask
    - best offer
  - name: ask_volume_0
    type: INTEGER
    description: Volume at the best ask price (level 0) for the option at the time of the OTO swing trade. Indicates available
      liquidity on the ask side.
    synonyms:
    - ask size 0
    - best ask size
  - name: ul_bid_price_0
    type: FLOAT
    description: Underlying bid price at depth level 0 at the time of the OTO swing trade. Level 0 is the best bid; higher
      levels represent deeper book. Used for underlying liquidity analysis.
    synonyms:
    - ul bid 0
  - name: ul_bid_volume_0
    type: INTEGER
    description: Underlying bid volume at depth level 0 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_ask_price_0
    type: FLOAT
    description: Underlying ask price at depth level 0 at the time of the OTO swing trade. Level 0 is the best ask; higher
      levels represent deeper book.
    synonyms:
    - ul ask 0
  - name: ul_ask_volume_0
    type: INTEGER
    description: Underlying ask volume at depth level 0 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_bid_price_1
    type: FLOAT
    description: Underlying bid price at depth level 1 at the time of the OTO swing trade. Level 0 is the best bid; higher
      levels represent deeper book. Used for underlying liquidity analysis.
    synonyms: []
  - name: ul_bid_volume_1
    type: INTEGER
    description: Underlying bid volume at depth level 1 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_ask_price_1
    type: FLOAT
    description: Underlying ask price at depth level 1 at the time of the OTO swing trade. Level 0 is the best ask; higher
      levels represent deeper book.
    synonyms: []
  - name: ul_ask_volume_1
    type: INTEGER
    description: Underlying ask volume at depth level 1 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_bid_price_2
    type: FLOAT
    description: Underlying bid price at depth level 2 at the time of the OTO swing trade. Level 0 is the best bid; higher
      levels represent deeper book. Used for underlying liquidity analysis.
    synonyms: []
  - name: ul_bid_volume_2
    type: INTEGER
    description: Underlying bid volume at depth level 2 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_ask_price_2
    type: FLOAT
    description: Underlying ask price at depth level 2 at the time of the OTO swing trade. Level 0 is the best ask; higher
      levels represent deeper book.
    synonyms: []
  - name: ul_ask_volume_2
    type: INTEGER
    description: Underlying ask volume at depth level 2 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_bid_price_3
    type: FLOAT
    description: Underlying bid price at depth level 3 at the time of the OTO swing trade. Level 0 is the best bid; higher
      levels represent deeper book. Used for underlying liquidity analysis.
    synonyms: []
  - name: ul_bid_volume_3
    type: INTEGER
    description: Underlying bid volume at depth level 3 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_ask_price_3
    type: FLOAT
    description: Underlying ask price at depth level 3 at the time of the OTO swing trade. Level 0 is the best ask; higher
      levels represent deeper book.
    synonyms: []
  - name: ul_ask_volume_3
    type: INTEGER
    description: Underlying ask volume at depth level 3 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_bid_price_4
    type: FLOAT
    description: Underlying bid price at depth level 4 at the time of the OTO swing trade. Level 0 is the best bid; higher
      levels represent deeper book. Used for underlying liquidity analysis.
    synonyms: []
  - name: ul_bid_volume_4
    type: INTEGER
    description: Underlying bid volume at depth level 4 at the time of the OTO swing trade.
    synonyms: []
  - name: ul_ask_price_4
    type: FLOAT
    description: Underlying ask price at depth level 4 at the time of the OTO swing trade. Level 0 is the best ask; higher
      levels represent deeper book.
    synonyms: []
  - name: ul_ask_volume_4
    type: INTEGER
    description: Underlying ask volume at depth level 4 at the time of the OTO swing trade.
    synonyms: []
  - name: adjusted_roll
    type: FLOAT
    description: Roll value after adjustments have been applied, accounting for any systematic corrections to the cost-of-carry
      calculation.
    synonyms:
    - adj roll
  - name: front_month_roll
    type: STRING
    description: Front month roll value or indicator, relevant for instruments near the front-month futures roll. Affects
      cost-of-carry and forward pricing calculations.
    synonyms:
    - front roll
  - name: curve_id_1s
    type: INTEGER
    description: Volatility curve identifier at the 1 second forward snapshot.
    synonyms: []
  - name: carry_id_1s
    type: INTEGER
    description: Carry/dividend curve identifier at the 1 second forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_1s
    type: INTEGER
    description: Market depth sequence number at the 1 second forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_1s
    type: BOOLEAN
    description: Whether the 1 second market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_1s
    type: BOOLEAN
    description: Whether the 1 second theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_1s
    type: TIMESTAMP
    description: Timestamp of the event at the 1 second forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_1s
    type: INTEGER
    description: Nanosecond timestamp at the 1 second forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_1s
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 1 second.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_1s
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 1 second.
    synonyms: []
  - name: theo_event_tx_timestamp_1s
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 1 second forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_1s
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 1 second.
    synonyms: []
  - name: when_timestamp_1s
    type: TIMESTAMP
    description: When-timestamp at the 1 second forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_1s
    type: INTEGER
    description: Nanosecond when-timestamp at 1 second.
    synonyms: []
  - name: ul_bid_price_0_1s
    type: FLOAT
    description: Underlying best bid price at the 1 second forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_1s
    type: INTEGER
    description: Underlying bid volume at level 0 at the 1 second forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_1s
    type: FLOAT
    description: Underlying best ask price at the 1 second forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_1s
    type: INTEGER
    description: Underlying ask volume at level 0 at the 1 second forward snapshot.
    synonyms: []
  - name: calculated_base_val_1s
    type: FLOAT
    description: Calculated base (underlying) value at the 1 second forward snapshot.
    synonyms: []
  - name: tv_offset_1s
    type: FLOAT
    description: TV offset applied at the 1 second forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_1s
    type: FLOAT
    description: Raw delta mid base value at the 1 second forward snapshot.
    synonyms: []
  - name: ref_base_val_1s
    type: FLOAT
    description: Reference base value at the 1 second forward snapshot.
    synonyms: []
  - name: curve_id_5s
    type: INTEGER
    description: Volatility curve identifier at the 5 seconds forward snapshot.
    synonyms: []
  - name: carry_id_5s
    type: INTEGER
    description: Carry/dividend curve identifier at the 5 seconds forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_5s
    type: INTEGER
    description: Market depth sequence number at the 5 seconds forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_5s
    type: BOOLEAN
    description: Whether the 5 seconds market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_5s
    type: BOOLEAN
    description: Whether the 5 seconds theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_5s
    type: TIMESTAMP
    description: Timestamp of the event at the 5 seconds forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_5s
    type: INTEGER
    description: Nanosecond timestamp at the 5 seconds forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_5s
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 5 seconds.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_5s
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 5 seconds.
    synonyms: []
  - name: theo_event_tx_timestamp_5s
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 5 seconds forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_5s
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 5 seconds.
    synonyms: []
  - name: when_timestamp_5s
    type: TIMESTAMP
    description: When-timestamp at the 5 seconds forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_5s
    type: INTEGER
    description: Nanosecond when-timestamp at 5 seconds.
    synonyms: []
  - name: ul_bid_price_0_5s
    type: FLOAT
    description: Underlying best bid price at the 5 seconds forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_5s
    type: INTEGER
    description: Underlying bid volume at level 0 at the 5 seconds forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_5s
    type: FLOAT
    description: Underlying best ask price at the 5 seconds forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_5s
    type: INTEGER
    description: Underlying ask volume at level 0 at the 5 seconds forward snapshot.
    synonyms: []
  - name: calculated_base_val_5s
    type: FLOAT
    description: Calculated base (underlying) value at the 5 seconds forward snapshot.
    synonyms: []
  - name: tv_offset_5s
    type: FLOAT
    description: TV offset applied at the 5 seconds forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_5s
    type: FLOAT
    description: Raw delta mid base value at the 5 seconds forward snapshot.
    synonyms: []
  - name: ref_base_val_5s
    type: FLOAT
    description: Reference base value at the 5 seconds forward snapshot.
    synonyms: []
  - name: curve_id_10s
    type: INTEGER
    description: Volatility curve identifier at the 10 seconds forward snapshot.
    synonyms: []
  - name: carry_id_10s
    type: INTEGER
    description: Carry/dividend curve identifier at the 10 seconds forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_10s
    type: INTEGER
    description: Market depth sequence number at the 10 seconds forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_10s
    type: BOOLEAN
    description: Whether the 10 seconds market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_10s
    type: BOOLEAN
    description: Whether the 10 seconds theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_10s
    type: TIMESTAMP
    description: Timestamp of the event at the 10 seconds forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_10s
    type: INTEGER
    description: Nanosecond timestamp at the 10 seconds forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_10s
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 10 seconds.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_10s
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 10 seconds.
    synonyms: []
  - name: theo_event_tx_timestamp_10s
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 10 seconds forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_10s
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 10 seconds.
    synonyms: []
  - name: when_timestamp_10s
    type: TIMESTAMP
    description: When-timestamp at the 10 seconds forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_10s
    type: INTEGER
    description: Nanosecond when-timestamp at 10 seconds.
    synonyms: []
  - name: ul_bid_price_0_10s
    type: FLOAT
    description: Underlying best bid price at the 10 seconds forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_10s
    type: INTEGER
    description: Underlying bid volume at level 0 at the 10 seconds forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_10s
    type: FLOAT
    description: Underlying best ask price at the 10 seconds forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_10s
    type: INTEGER
    description: Underlying ask volume at level 0 at the 10 seconds forward snapshot.
    synonyms: []
  - name: calculated_base_val_10s
    type: FLOAT
    description: Calculated base (underlying) value at the 10 seconds forward snapshot.
    synonyms: []
  - name: tv_offset_10s
    type: FLOAT
    description: TV offset applied at the 10 seconds forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_10s
    type: FLOAT
    description: Raw delta mid base value at the 10 seconds forward snapshot.
    synonyms: []
  - name: ref_base_val_10s
    type: FLOAT
    description: Reference base value at the 10 seconds forward snapshot.
    synonyms: []
  - name: curve_id_30s
    type: INTEGER
    description: Volatility curve identifier at the 30 seconds forward snapshot.
    synonyms: []
  - name: carry_id_30s
    type: INTEGER
    description: Carry/dividend curve identifier at the 30 seconds forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_30s
    type: INTEGER
    description: Market depth sequence number at the 30 seconds forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_30s
    type: BOOLEAN
    description: Whether the 30 seconds market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_30s
    type: BOOLEAN
    description: Whether the 30 seconds theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_30s
    type: TIMESTAMP
    description: Timestamp of the event at the 30 seconds forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_30s
    type: INTEGER
    description: Nanosecond timestamp at the 30 seconds forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_30s
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 30 seconds.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_30s
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 30 seconds.
    synonyms: []
  - name: theo_event_tx_timestamp_30s
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 30 seconds forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_30s
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 30 seconds.
    synonyms: []
  - name: when_timestamp_30s
    type: TIMESTAMP
    description: When-timestamp at the 30 seconds forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_30s
    type: INTEGER
    description: Nanosecond when-timestamp at 30 seconds.
    synonyms: []
  - name: ul_bid_price_0_30s
    type: FLOAT
    description: Underlying best bid price at the 30 seconds forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_30s
    type: INTEGER
    description: Underlying bid volume at level 0 at the 30 seconds forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_30s
    type: FLOAT
    description: Underlying best ask price at the 30 seconds forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_30s
    type: INTEGER
    description: Underlying ask volume at level 0 at the 30 seconds forward snapshot.
    synonyms: []
  - name: calculated_base_val_30s
    type: FLOAT
    description: Calculated base (underlying) value at the 30 seconds forward snapshot.
    synonyms: []
  - name: tv_offset_30s
    type: FLOAT
    description: TV offset applied at the 30 seconds forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_30s
    type: FLOAT
    description: Raw delta mid base value at the 30 seconds forward snapshot.
    synonyms: []
  - name: ref_base_val_30s
    type: FLOAT
    description: Reference base value at the 30 seconds forward snapshot.
    synonyms: []
  - name: curve_id_1m
    type: INTEGER
    description: Volatility curve identifier at the 1 minute forward snapshot.
    synonyms: []
  - name: carry_id_1m
    type: INTEGER
    description: Carry/dividend curve identifier at the 1 minute forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_1m
    type: INTEGER
    description: Market depth sequence number at the 1 minute forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_1m
    type: BOOLEAN
    description: Whether the 1 minute market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_1m
    type: BOOLEAN
    description: Whether the 1 minute theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_1m
    type: TIMESTAMP
    description: Timestamp of the event at the 1 minute forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_1m
    type: INTEGER
    description: Nanosecond timestamp at the 1 minute forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_1m
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 1 minute.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_1m
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 1 minute.
    synonyms: []
  - name: theo_event_tx_timestamp_1m
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 1 minute forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_1m
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 1 minute.
    synonyms: []
  - name: when_timestamp_1m
    type: TIMESTAMP
    description: When-timestamp at the 1 minute forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_1m
    type: INTEGER
    description: Nanosecond when-timestamp at 1 minute.
    synonyms: []
  - name: ul_bid_price_0_1m
    type: FLOAT
    description: Underlying best bid price at the 1 minute forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_1m
    type: INTEGER
    description: Underlying bid volume at level 0 at the 1 minute forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_1m
    type: FLOAT
    description: Underlying best ask price at the 1 minute forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_1m
    type: INTEGER
    description: Underlying ask volume at level 0 at the 1 minute forward snapshot.
    synonyms: []
  - name: calculated_base_val_1m
    type: FLOAT
    description: Calculated base (underlying) value at the 1 minute forward snapshot.
    synonyms: []
  - name: tv_offset_1m
    type: FLOAT
    description: TV offset applied at the 1 minute forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_1m
    type: FLOAT
    description: Raw delta mid base value at the 1 minute forward snapshot.
    synonyms: []
  - name: ref_base_val_1m
    type: FLOAT
    description: Reference base value at the 1 minute forward snapshot.
    synonyms: []
  - name: curve_id_5m
    type: INTEGER
    description: Volatility curve identifier at the 5 minutes forward snapshot.
    synonyms: []
  - name: carry_id_5m
    type: INTEGER
    description: Carry/dividend curve identifier at the 5 minutes forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_5m
    type: INTEGER
    description: Market depth sequence number at the 5 minutes forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_5m
    type: BOOLEAN
    description: Whether the 5 minutes market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_5m
    type: BOOLEAN
    description: Whether the 5 minutes theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_5m
    type: TIMESTAMP
    description: Timestamp of the event at the 5 minutes forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_5m
    type: INTEGER
    description: Nanosecond timestamp at the 5 minutes forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_5m
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 5 minutes.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_5m
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 5 minutes.
    synonyms: []
  - name: theo_event_tx_timestamp_5m
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 5 minutes forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_5m
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 5 minutes.
    synonyms: []
  - name: when_timestamp_5m
    type: TIMESTAMP
    description: When-timestamp at the 5 minutes forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_5m
    type: INTEGER
    description: Nanosecond when-timestamp at 5 minutes.
    synonyms: []
  - name: ul_bid_price_0_5m
    type: FLOAT
    description: Underlying best bid price at the 5 minutes forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_5m
    type: INTEGER
    description: Underlying bid volume at level 0 at the 5 minutes forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_5m
    type: FLOAT
    description: Underlying best ask price at the 5 minutes forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_5m
    type: INTEGER
    description: Underlying ask volume at level 0 at the 5 minutes forward snapshot.
    synonyms: []
  - name: calculated_base_val_5m
    type: FLOAT
    description: Calculated base (underlying) value at the 5 minutes forward snapshot.
    synonyms: []
  - name: tv_offset_5m
    type: FLOAT
    description: TV offset applied at the 5 minutes forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_5m
    type: FLOAT
    description: Raw delta mid base value at the 5 minutes forward snapshot.
    synonyms: []
  - name: ref_base_val_5m
    type: FLOAT
    description: Reference base value at the 5 minutes forward snapshot.
    synonyms: []
  - name: curve_id_10m
    type: INTEGER
    description: Volatility curve identifier at the 10 minutes forward snapshot.
    synonyms: []
  - name: carry_id_10m
    type: INTEGER
    description: Carry/dividend curve identifier at the 10 minutes forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_10m
    type: INTEGER
    description: Market depth sequence number at the 10 minutes forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_10m
    type: BOOLEAN
    description: Whether the 10 minutes market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_10m
    type: BOOLEAN
    description: Whether the 10 minutes theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_10m
    type: TIMESTAMP
    description: Timestamp of the event at the 10 minutes forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_10m
    type: INTEGER
    description: Nanosecond timestamp at the 10 minutes forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_10m
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 10 minutes.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_10m
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 10 minutes.
    synonyms: []
  - name: theo_event_tx_timestamp_10m
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 10 minutes forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_10m
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 10 minutes.
    synonyms: []
  - name: when_timestamp_10m
    type: TIMESTAMP
    description: When-timestamp at the 10 minutes forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_10m
    type: INTEGER
    description: Nanosecond when-timestamp at 10 minutes.
    synonyms: []
  - name: ul_bid_price_0_10m
    type: FLOAT
    description: Underlying best bid price at the 10 minutes forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_10m
    type: INTEGER
    description: Underlying bid volume at level 0 at the 10 minutes forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_10m
    type: FLOAT
    description: Underlying best ask price at the 10 minutes forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_10m
    type: INTEGER
    description: Underlying ask volume at level 0 at the 10 minutes forward snapshot.
    synonyms: []
  - name: calculated_base_val_10m
    type: FLOAT
    description: Calculated base (underlying) value at the 10 minutes forward snapshot.
    synonyms: []
  - name: tv_offset_10m
    type: FLOAT
    description: TV offset applied at the 10 minutes forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_10m
    type: FLOAT
    description: Raw delta mid base value at the 10 minutes forward snapshot.
    synonyms: []
  - name: ref_base_val_10m
    type: FLOAT
    description: Reference base value at the 10 minutes forward snapshot.
    synonyms: []
  - name: curve_id_30m
    type: INTEGER
    description: Volatility curve identifier at the 30 minutes forward snapshot.
    synonyms: []
  - name: carry_id_30m
    type: INTEGER
    description: Carry/dividend curve identifier at the 30 minutes forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_30m
    type: INTEGER
    description: Market depth sequence number at the 30 minutes forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_30m
    type: BOOLEAN
    description: Whether the 30 minutes market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_30m
    type: BOOLEAN
    description: Whether the 30 minutes theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_30m
    type: TIMESTAMP
    description: Timestamp of the event at the 30 minutes forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_30m
    type: INTEGER
    description: Nanosecond timestamp at the 30 minutes forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_30m
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 30 minutes.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_30m
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 30 minutes.
    synonyms: []
  - name: theo_event_tx_timestamp_30m
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 30 minutes forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_30m
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 30 minutes.
    synonyms: []
  - name: when_timestamp_30m
    type: TIMESTAMP
    description: When-timestamp at the 30 minutes forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_30m
    type: INTEGER
    description: Nanosecond when-timestamp at 30 minutes.
    synonyms: []
  - name: ul_bid_price_0_30m
    type: FLOAT
    description: Underlying best bid price at the 30 minutes forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_30m
    type: INTEGER
    description: Underlying bid volume at level 0 at the 30 minutes forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_30m
    type: FLOAT
    description: Underlying best ask price at the 30 minutes forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_30m
    type: INTEGER
    description: Underlying ask volume at level 0 at the 30 minutes forward snapshot.
    synonyms: []
  - name: calculated_base_val_30m
    type: FLOAT
    description: Calculated base (underlying) value at the 30 minutes forward snapshot.
    synonyms: []
  - name: tv_offset_30m
    type: FLOAT
    description: TV offset applied at the 30 minutes forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_30m
    type: FLOAT
    description: Raw delta mid base value at the 30 minutes forward snapshot.
    synonyms: []
  - name: ref_base_val_30m
    type: FLOAT
    description: Reference base value at the 30 minutes forward snapshot.
    synonyms: []
  - name: curve_id_1h
    type: INTEGER
    description: Volatility curve identifier at the 1 hour forward snapshot.
    synonyms: []
  - name: carry_id_1h
    type: INTEGER
    description: Carry/dividend curve identifier at the 1 hour forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_1h
    type: INTEGER
    description: Market depth sequence number at the 1 hour forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_1h
    type: BOOLEAN
    description: Whether the 1 hour market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_1h
    type: BOOLEAN
    description: Whether the 1 hour theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_1h
    type: TIMESTAMP
    description: Timestamp of the event at the 1 hour forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_1h
    type: INTEGER
    description: Nanosecond timestamp at the 1 hour forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_1h
    type: TIMESTAMP
    description: Timestamp of the calculated base value at 1 hour.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_1h
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at 1 hour.
    synonyms: []
  - name: theo_event_tx_timestamp_1h
    type: TIMESTAMP
    description: Theo event transaction timestamp at the 1 hour forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_1h
    type: INTEGER
    description: Nanosecond theo event tx timestamp at 1 hour.
    synonyms: []
  - name: when_timestamp_1h
    type: TIMESTAMP
    description: When-timestamp at the 1 hour forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_1h
    type: INTEGER
    description: Nanosecond when-timestamp at 1 hour.
    synonyms: []
  - name: ul_bid_price_0_1h
    type: FLOAT
    description: Underlying best bid price at the 1 hour forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_1h
    type: INTEGER
    description: Underlying bid volume at level 0 at the 1 hour forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_1h
    type: FLOAT
    description: Underlying best ask price at the 1 hour forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_1h
    type: INTEGER
    description: Underlying ask volume at level 0 at the 1 hour forward snapshot.
    synonyms: []
  - name: calculated_base_val_1h
    type: FLOAT
    description: Calculated base (underlying) value at the 1 hour forward snapshot.
    synonyms: []
  - name: tv_offset_1h
    type: FLOAT
    description: TV offset applied at the 1 hour forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_1h
    type: FLOAT
    description: Raw delta mid base value at the 1 hour forward snapshot.
    synonyms: []
  - name: ref_base_val_1h
    type: FLOAT
    description: Reference base value at the 1 hour forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_eod
    type: INTEGER
    description: Market depth sequence number at the end-of-day forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_eod
    type: BOOLEAN
    description: Whether the end-of-day market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_eod
    type: STRING
    description: Whether the end-of-day theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_eod
    type: TIMESTAMP
    description: Timestamp of the event at the end-of-day forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_eod
    type: INTEGER
    description: Nanosecond timestamp at the end-of-day forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_eod
    type: TIMESTAMP
    description: Timestamp of the calculated base value at end-of-day.
    synonyms: []
  - name: calculated_base_val_timestamp_ns_eod
    type: INTEGER
    description: Nanosecond timestamp of calculated base value at end-of-day.
    synonyms: []
  - name: theo_event_tx_timestamp_eod
    type: TIMESTAMP
    description: Theo event transaction timestamp at the end-of-day forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_eod
    type: INTEGER
    description: Nanosecond theo event tx timestamp at end-of-day.
    synonyms: []
  - name: when_timestamp_eod
    type: STRING
    description: When-timestamp at the end-of-day forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_eod
    type: STRING
    description: Nanosecond when-timestamp at end-of-day.
    synonyms: []
  - name: ul_bid_price_0_eod
    type: FLOAT
    description: Underlying best bid price at the end-of-day forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_eod
    type: INTEGER
    description: Underlying bid volume at level 0 at the end-of-day forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_eod
    type: FLOAT
    description: Underlying best ask price at the end-of-day forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_eod
    type: INTEGER
    description: Underlying ask volume at level 0 at the end-of-day forward snapshot.
    synonyms: []
  - name: tv_offset_eod
    type: FLOAT
    description: TV offset applied at the end-of-day forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_eod
    type: FLOAT
    description: Raw delta mid base value at the end-of-day forward snapshot.
    synonyms: []
  - name: ref_base_val_eod
    type: FLOAT
    description: Reference base value at the end-of-day forward snapshot.
    synonyms: []
  - name: curve_id_1D
    type: STRING
    description: Volatility curve identifier at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: carry_id_1D
    type: STRING
    description: Carry/dividend curve identifier at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_1D
    type: STRING
    description: Market depth sequence number at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_1D
    type: STRING
    description: Whether the 1 day (next open) market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_1D
    type: STRING
    description: Whether the 1 day (next open) theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_1D
    type: TIMESTAMP
    description: Timestamp of the event at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_1D
    type: INTEGER
    description: Nanosecond timestamp at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_1D
    type: STRING
    description: Timestamp of the calculated base value at 1 day (next open).
    synonyms: []
  - name: calculated_base_val_timestamp_ns_1D
    type: STRING
    description: Nanosecond timestamp of calculated base value at 1 day (next open).
    synonyms: []
  - name: theo_event_tx_timestamp_1D
    type: STRING
    description: Theo event transaction timestamp at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_1D
    type: STRING
    description: Nanosecond theo event tx timestamp at 1 day (next open).
    synonyms: []
  - name: when_timestamp_1D
    type: STRING
    description: When-timestamp at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_1D
    type: STRING
    description: Nanosecond when-timestamp at 1 day (next open).
    synonyms: []
  - name: ul_bid_price_0_1D
    type: STRING
    description: Underlying best bid price at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_1D
    type: STRING
    description: Underlying bid volume at level 0 at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_1D
    type: STRING
    description: Underlying best ask price at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_1D
    type: STRING
    description: Underlying ask volume at level 0 at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: tv_offset_1D
    type: STRING
    description: TV offset applied at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_1D
    type: STRING
    description: Raw delta mid base value at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: ref_base_val_1D
    type: STRING
    description: Reference base value at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: curve_id_5D
    type: STRING
    description: Volatility curve identifier at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: carry_id_5D
    type: STRING
    description: Carry/dividend curve identifier at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: mdepth_sequence_number_5D
    type: STRING
    description: Market depth sequence number at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: is_mdepth_snapshot_5D
    type: STRING
    description: Whether the 5 days (T+5 open) market depth data was from a periodic snapshot.
    synonyms: []
  - name: is_theodata_snapshot_5D
    type: STRING
    description: Whether the 5 days (T+5 open) theo data was from a periodic snapshot.
    synonyms: []
  - name: event_timestamp_5D
    type: TIMESTAMP
    description: Timestamp of the event at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: event_timestamp_ns_5D
    type: INTEGER
    description: Nanosecond timestamp at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: calculated_base_val_timestamp_5D
    type: STRING
    description: Timestamp of the calculated base value at 5 days (T+5 open).
    synonyms: []
  - name: calculated_base_val_timestamp_ns_5D
    type: STRING
    description: Nanosecond timestamp of calculated base value at 5 days (T+5 open).
    synonyms: []
  - name: theo_event_tx_timestamp_5D
    type: STRING
    description: Theo event transaction timestamp at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: theo_event_tx_timestamp_ns_5D
    type: STRING
    description: Nanosecond theo event tx timestamp at 5 days (T+5 open).
    synonyms: []
  - name: when_timestamp_5D
    type: STRING
    description: When-timestamp at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: when_timestamp_ns_5D
    type: STRING
    description: Nanosecond when-timestamp at 5 days (T+5 open).
    synonyms: []
  - name: ul_bid_price_0_5D
    type: STRING
    description: Underlying best bid price at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: ul_bid_volume_0_5D
    type: STRING
    description: Underlying bid volume at level 0 at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: ul_ask_price_0_5D
    type: STRING
    description: Underlying best ask price at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: ul_ask_volume_0_5D
    type: STRING
    description: Underlying ask volume at level 0 at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: tv_offset_5D
    type: STRING
    description: TV offset applied at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: raw_delta_mid_bv_5D
    type: STRING
    description: Raw delta mid base value at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: ref_base_val_5D
    type: STRING
    description: Reference base value at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: is_auction_marketdepth_1s
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 1 second was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_5s
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 5 seconds was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_10s
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 10 seconds was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_30s
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 30 seconds was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_1m
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 1 minute was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_5m
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 5 minutes was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_10m
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 10 minutes was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_30m
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 30 minutes was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_1h
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 1 hour was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_eod
    type: BOOLEAN
    description: Boolean indicating whether the market depth at end-of-day was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_1D
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 1 day (next open) was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth_5D
    type: BOOLEAN
    description: Boolean indicating whether the market depth at 5 days (T+5 open) was during an auction period.
    synonyms: []
  - name: is_auction_marketdepth
    type: BOOLEAN
    description: Boolean indicating whether the market depth data at the time of the trade was from an auction period. Auction
      market depth has different characteristics than continuous trading.
    synonyms: []
    business_rules: Auction depth may not be representative of continuous market conditions.
  - name: event_timestamp
    type: TIMESTAMP
    description: Timestamp of the OTO swing event at microsecond precision. Records when the swing opportunity was detected
      and the order routing decision was made. Use event_timestamp_ns for nanosecond precision. Useful for time-of-day analysis
      and latency measurement.
    synonyms:
    - event time
    - timestamp
    - trade time
    source: 'SwingData.proto :: VtCommon.props'
  - name: delta_adjusted_base_price
    type: FLOAT
    description: Delta-adjusted base (underlying) price used in KPI calculations. This is the reference underlying price adjusted
      for the option's delta sensitivity, serving as the anchor point for slippage decomposition. Changes in this value drive
      delta_slippage components.
    synonyms:
    - delta adjusted_base_price
    - adjusted base
    - delta adj base
    source: kpi_otoswing_calculations.sql
  - name: exchange
    type: STRING
    description: Exchange or venue where this OTO swing trade was executed. Identifies the matching engine that filled the
      order. Used for venue analysis and to understand execution quality by exchange.
    synonyms:
    - venue
    - market
    - trading venue
    - execution venue
  - name: swing_mid_base_val
    type: FLOAT
    description: Mid-point base (underlying) value used in the swing theoretical value calculation. Represents the estimated
      fair underlying price at the midpoint of the sweep, accounting for the layered book. Used as input to the swing_mid_tv
      second-order Taylor expansion.
    synonyms:
    - swing mid underlying
    - mid base value
    source: kpi_otoswing_calculations.sql
    formula: '(raw_bv_bid + raw_bv_ask) / 2'
  - name: raw_delta_mid_bv
    type: FLOAT
    description: Raw (unadjusted) delta of the mid base value at the time of the OTO swing trade. Used internally in the pricing
      pipeline before adjustments are applied.
    synonyms:
    - raw mid delta
    formula: 'raw_delta_mid_bv + (((raw_bv_bid + raw_bv_ask) / 2) - (under - roll)) * raw_gamma'
  - name: base_adjustment
    type: FLOAT
    description: Adjustment applied to the base (underlying) price for this OTO swing trade. Captures any systematic corrections
      or offsets applied to the raw underlying price before KPI calculations.
    synonyms:
    - base adj
    - underlying adjustment
    source: kpi_otoswing_calculations.sql
    formula: 'layered_base_adjustment + odr_adjustment'
  - name: buy_sell_multiplier
    type: INTEGER
    description: 'Integer multiplier indicating trade direction: +1 for buy, -1 for sell. Used in all PnL and edge calculations
      to correctly sign the result based on direction. Essential for directional P&L attribution.'
    synonyms:
    - direction multiplier
    - side multiplier
    - bs multiplier
    business_rules: +1 = buy, -1 = sell. Multiply by edge to get directional PnL.
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN 1 ELSE -1 END"
  - name: tv
    type: FLOAT
    description: Theoretical value (fair value) of the option at the time of the OTO swing trade, as computed by the pricing
      engine. Expressed in the instrument's native price units. This is the system's best estimate of the option's true worth.
      Traders call this theo, fair value, or machine price. Central to all edge and PnL calculations.
    synonyms:
    - theo
    - theoretical value
    - fair value
    - machine price
    - TV
    business_rules: Primary fair-value reference for all instant_edge calculations. Check is_ref_theo_invalid for data quality.
    source: kpi_otoswing_normalized.sql
    formula: 'CASE WHEN parent_is_combo = TRUE AND is_parent = TRUE THEN oto_tv WHEN parent_is_combo = TRUE AND is_parent = FALSE THEN tv_theo ELSE oto_tv END'
  - name: swing_mid_tv
    type: FLOAT
    description: Theoretical value computed using the swing mid base value instead of the standard delta-adjusted base price.
      This KPI-added field uses a second-order Taylor expansion to estimate fair value at the swing midpoint, accounting for
      both delta and gamma effects. Used as an alternative fair-value anchor for fired_at mid_tv PnL calculations.
    synonyms:
    - swing mid theo
    - mid tv swing
    formula: tv + delta * (swing_mid_base_val - delta_adjusted_base_price) + 0.5 * ref_gamma * POW((swing_mid_base_val - delta_adjusted_base_price),
      2)
    business_rules: Uses second-order Taylor expansion. Only meaningful when gamma is material (near-ATM, short-dated options).
    source: kpi_otoswing_calculations.sql (KPI-Added)
  - name: routed_buy_sell_normalized
    type: STRING
    description: Normalized buy/sell direction of the routed OTO swing order, cleaned from the raw proto enum into a standardized
      string. Use this field instead of routed_buy_sell for filtering and grouping by trade direction.
    synonyms:
    - normalized direction
    - clean side
    business_rules: Derived from routed_buy_sell. Prefer this over the raw field for queries.
    source: kpi_otoswing_normalized.sql
  - name: instant_edge
    type: FLOAT
    description: Instantaneous edge at the time of the OTO swing trade in ticks per unit, measuring theoretical profit before
      any forward-looking slippage. Computed as the directional difference between TV (adjusted for NHR) and the average trade
      price. This is the primary edge metric. Positive = profitable. Also known as edge, trading edge, or edge_bps.
    synonyms:
    - edge
    - trading edge
    - edge_bps
    - instant profit per unit
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN (CASE WHEN trade_volume > 0 THEN ((tv - nhr_adjustment_bid) - avg_trade_price) ELSE 0 END) ELSE (CASE WHEN trade_volume > 0 THEN (avg_trade_price - (tv - nhr_adjustment_bid)) ELSE 0 END) END"
    business_rules: Based on avg_trade_price. Compare with instant_edge_fired_at for routed price version.
    source: kpi_otoswing_calculations.sql (KPI-Added)
  - name: instant_edge_fired_at
    type: FLOAT
    description: Instantaneous edge measured at the routed (fired) price rather than the average fill price, in ticks per
      unit. Compares the theoretical value (adjusted for NHR) against routed_price to measure how much edge was expected at
      the moment the order was sent. Traders call this fire edge. Positive means the order was sent at a favorable price relative
      to theo.
    synonyms:
    - fire edge
    - fired edge
    - edge at fire
    - routed edge
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN ((tv - nhr_adjustment_bid) - routed_price) ELSE (routed_price - (tv - nhr_adjustment_bid)) END"
    business_rules: 'Direction-aware: for buys, edge = theo - price; for sells, edge = price - theo. Uses NHR-adjusted TV.'
    source: kpi_otoswing_calculations.sql (KPI-Added)
  - name: instant_pnl
    type: FLOAT
    description: Instantaneous profit and loss at the time of the OTO swing trade, in currency units. Computed as instant_edge
      multiplied by trade_volume and contract_size. Represents the theoretical profit at trade time before any forward-looking
      price movement. Also known as PnL, profit, or instant profit.
    synonyms:
    - PnL
    - profit
    - loss
    - instant profit
    - trade pnl
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN ((tv - nhr_adjustment_bid) - avg_trade_price) * trade_volume * contract_size ELSE (avg_trade_price - (tv - nhr_adjustment_bid)) * trade_volume * contract_size END"
    business_rules: Based on trade_volume (filled). Compare with instant_pnl_fired_at for routed size version.
    source: kpi_otoswing_calculations.sql (KPI-Added)
  - name: instant_pnl_fired_at
    type: FLOAT
    description: Instantaneous PnL at the routed (fired) price, computed as instant_edge_fired_at multiplied by routed_size
      and contract_size. Represents the expected profit at the moment the order was sent, in currency units. Traders call
      this fire PnL. Differs from instant_pnl which uses avg_trade_price and trade_volume.
    synonyms:
    - fire pnl
    - fired pnl
    - pnl at fire
    - routed pnl
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN ((tv - nhr_adjustment_bid) - routed_price) * routed_size_capped * contract_size ELSE (routed_price - (tv - nhr_adjustment_bid)) * routed_size_capped * contract_size END"
    business_rules: Based on routed_size, not trade_volume. Compare with instant_pnl to measure execution slippage vs decision.
    source: kpi_otoswing_calculations.sql (KPI-Added)
  - name: instant_pnl_w_fees
    type: FLOAT
    description: Instantaneous PnL including exchange and clearing fees deducted. The net profitability of the trade at execution
      time. Computed as instant_pnl minus fees. This is the most realistic instant profitability metric. Also called PnL with
      fees or net PnL.
    synonyms:
    - PnL with fees
    - net PnL
    - profit after fees
    - net instant pnl
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN (((tv - nhr_adjustment_bid) - avg_trade_price) - fees) * trade_volume * contract_size ELSE ((avg_trade_price - (tv - nhr_adjustment_bid)) - fees) * trade_volume * contract_size END"
    source: kpi_otoswing_calculations.sql (KPI-Added)
  - name: instant_pnl_fired_at_w_fees
    type: FLOAT
    description: Instantaneous PnL at the fired (routed) price including exchange and clearing fees, using routed_size as
      the quantity multiplier. The net version of instant_pnl_fired_at after all fee deductions. Gives the true net profitability
      at the decision point.
    synonyms:
    - fire net pnl
    - fired pnl with fees
    - net fire pnl
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN (((tv - nhr_adjustment_bid) - routed_price) - fees) * routed_size_capped * contract_size ELSE ((routed_price - (tv - nhr_adjustment_bid)) - fees) * routed_size_capped * contract_size END"
  - name: instant_edge_mid_tv
    type: FLOAT
    description: Instantaneous edge computed using swing_mid_tv as the fair value anchor instead of the standard TV. Measures
      how much edge was captured relative to the swing midpoint theoretical value. Provides an alternative edge metric that
      accounts for gamma effects.
    synonyms:
    - mid tv edge
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN (CASE WHEN trade_volume > 0 THEN ((swing_mid_tv - nhr_adjustment_bid) - avg_trade_price) ELSE 0 END) ELSE (CASE WHEN trade_volume > 0 THEN (avg_trade_price - (swing_mid_tv - nhr_adjustment_bid)) ELSE 0 END) END"
    source: kpi_otoswing_calculations.sql (KPI-Added)
  - name: instant_edge_fired_at_mid_tv
    type: FLOAT
    description: Instantaneous edge at the fired (routed) price using swing_mid_tv as the fair value anchor. Combines the
      fired_at pricing with the mid-TV valuation for a gamma-aware measure of edge at the decision point.
    synonyms:
    - fire edge mid tv
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN ((swing_mid_tv - nhr_adjustment_bid) - routed_price) ELSE (routed_price - (swing_mid_tv - nhr_adjustment_bid)) END"
  - name: instant_pnl_mid_tv
    type: FLOAT
    description: Instantaneous PnL computed using swing_mid_tv as the fair value, multiplied by trade_volume and contract_size.
      Gives a gamma-adjusted PnL figure for the filled trade.
    synonyms:
    - mid tv pnl
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN ((swing_mid_tv - nhr_adjustment_bid) - avg_trade_price) * trade_volume * contract_size ELSE (avg_trade_price - (swing_mid_tv - nhr_adjustment_bid)) * trade_volume * contract_size END"
  - name: instant_pnl_fired_at_mid_tv
    type: FLOAT
    description: Instantaneous PnL at the fired price using swing_mid_tv, multiplied by routed_size and contract_size. Combines
      the fired_at sizing with gamma-adjusted valuation.
    synonyms:
    - fire pnl mid tv
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN ((swing_mid_tv - nhr_adjustment_bid) - routed_price) * routed_size_capped * contract_size ELSE (routed_price - (swing_mid_tv - nhr_adjustment_bid)) * routed_size_capped * contract_size END"
  - name: instant_pnl_w_fees_mid_tv
    type: FLOAT
    description: Instantaneous PnL using swing_mid_tv including exchange and clearing fees. The net version of instant_pnl_mid_tv
      after fee deductions.
    synonyms:
    - mid tv net pnl
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN (((swing_mid_tv - nhr_adjustment_bid) - avg_trade_price) - fees) * trade_volume * contract_size ELSE ((avg_trade_price - (swing_mid_tv - nhr_adjustment_bid)) - fees) * trade_volume * contract_size END"
  - name: instant_pnl_fired_at_w_fees_mid_tv
    type: FLOAT
    description: Instantaneous PnL at fired price using swing_mid_tv including fees. The net fired_at version of PnL after
      fee deductions with gamma-adjusted valuation.
    synonyms:
    - fire net pnl mid tv
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: "CASE WHEN routed_buy_sell IN ('BUYSELL_BUY', '66') THEN (((swing_mid_tv - nhr_adjustment_bid) - routed_price) - fees) * routed_size_capped * contract_size ELSE ((routed_price - (swing_mid_tv - nhr_adjustment_bid)) - fees) * routed_size_capped * contract_size END"
  - name: mark_to_mid_pnl_per_unit
    type: FLOAT
    description: Mark-to-mid PnL per unit, measuring the difference between the fill price and the mid-market theoretical
      value on a per-contract basis. Indicates execution quality relative to the mid-market fair value.
    synonyms:
    - m2m per unit
    - mark to mid per lot
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: '(tv - swing_mid_tv) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier * -1'
  - name: mark_to_mid_pnl
    type: FLOAT
    description: Total mark-to-mid PnL for this trade, computed as mark_to_mid_pnl_per_unit times trade_volume and contract_size.
      Measures total execution quality versus mid-market.
    synonyms:
    - m2m pnl
    - mark to mid
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: '(tv - swing_mid_tv) * trade_volume * contract_size * buy_sell_multiplier * -1'
  - name: mark_to_mid_pnl_fired_at
    type: FLOAT
    description: Mark-to-mid PnL based on the fired (routed) price and routed_size rather than the average fill price and
      trade_volume. Measures decision-point execution quality.
    synonyms:
    - fire m2m pnl
    - mark to mid fire
    source: kpi_otoswing_calculations.sql (KPI-Added)
    formula: '(tv - swing_mid_tv) * routed_size_capped * contract_size * buy_sell_multiplier * -1'
  - name: routed_buy_sell
    type: STRING
    description: Direction of the routed OTO swing order as a raw proto enum string. Values include BUYSELL_BUY (or numeric
      66) for buy orders and BUYSELL_SELL (or numeric 83) for sells. This is the raw proto value; use routed_buy_sell_normalized
      for a cleaned-up version.
    synonyms:
    - routed side
    - fire direction
    - routed direction
    business_rules: Raw proto enum. 0 or BUYSELL_BUY = buy, 1 or BUYSELL_SELL = sell. Use routed_buy_sell_normalized for clean
      values.
    source: 'SwingData.proto :: SwingData.routedBuySell'
    example_values:
    - BUYSELL_BUY
    - BUYSELL_SELL
    - '66'
    - '83'
  - name: vol_path_estimate_1s
    type: FLOAT
    description: Estimated volatility path effect at the 1 second horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 1 second after.
    synonyms: []
    formula: '((calculated_base_val_1s - swing_mid_base_val) * ((delta_1s - raw_delta_mid_bv_1s) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_1s_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 1 second horizon, measuring how much of the
      per-contract PnL change from trade time to 1 second after was attributable to implied volatility change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 1s
    - vol slip 1s per unit
    formula: '((vol_1s - vol) * ((vega_1s + vega) / 2) - vol_path_estimate_1s) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1s
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 1 second horizon, measuring the total vol-driven
      PnL change from trade time to 1 second after. Equals vol_slippage_1s_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 1s
    formula: '((vol_1s - vol) * ((vega_1s + vega) / 2) - vol_path_estimate_1s) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_1s_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 1 second horizon, measuring how much of
      the per-contract PnL change from trade time to 1 second after was attributable to residual factors not captured by delta,
      vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 1s
    - other slip 1s per unit
    formula: '(tv_change_buysell_1s * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_1s_per_unit + roll_slippage_1s_per_unit)'
  - name: other_slippage_1s
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 1 second horizon, measuring the total other-driven
      PnL change from trade time to 1 second after. Equals other_slippage_1s_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 1s
    formula: '(tv_change_buysell_1s * contract_size * trade_volume) - (delta_slippage_1s + roll_slippage_1s)'
  - name: tv_1s
    type: FLOAT
    description: Theoretical value (fair value) at 1 second after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 1s
  - name: delta_1s
    type: FLOAT
    description: Option delta at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 1s
  - name: gamma_1s
    type: FLOAT
    description: Option gamma at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_1s
    type: FLOAT
    description: Option vega at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_1s
    type: FLOAT
    description: Option theta at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_1s
    type: FLOAT
    description: Underlying price at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 1s
  - name: vol_1s
    type: FLOAT
    description: Implied volatility at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 1s
  - name: rho_1s
    type: FLOAT
    description: Option rho at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_1s
    type: FLOAT
    description: Raw (unadjusted) delta at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_1s
    type: FLOAT
    description: Raw (unadjusted) gamma at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_1s
    type: FLOAT
    description: Normalized strike (moneyness) at 1 second after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_1s
    type: FLOAT
    description: Roll adjustment value at 1 second after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_1s
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 1 second after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 1s
    formula: '(adjusted_tv_1s - swing_mid_tv)'
  - name: tv_change_buysell_1s
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 1 second, representing the realized PnL per unit from the TV
      movement. Positive = profitable direction.
    synonyms:
    - pnl 1s
    formula: '(adjusted_tv_1s - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_1s_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 1 second horizon, measuring how much of the per-contract
      PnL change from trade time to 1 second after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 1s
    - roll slip 1s per unit
    formula: '((reference_roll_1s - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1s
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 1 second horizon, measuring the total roll-driven PnL
      change from trade time to 1 second after. Equals roll_slippage_1s_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 1s
    formula: '((reference_roll_1s - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_1s
    type: FLOAT
    description: Adjusted theoretical value at 1 second after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_1s + delta_1s * (calculated_base_val_1s - (under_1s - roll_1s)) + 0.5 * gamma_1s * POW((calculated_base_val_1s - (under_1s - roll_1s)), 2)'
  - name: delta_slippage_1s_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 1 second horizon, measuring how much of the per-contract
      PnL change from trade time to 1 second after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 1s
    - delta slip 1s per unit
    formula: '((calculated_base_val_1s - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_1s
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 1 second horizon, measuring the total delta-driven PnL
      change from trade time to 1 second after. Equals delta_slippage_1s_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 1s
    formula: '((calculated_base_val_1s - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1s_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 1 second horizon, computed using routed_size (fired size) instead of
      trade_volume. Measures the volatility change contribution to PnL between trade time and 1 second after, based on the
      fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 1s
    - fire vol slip 1s
    - fire vol pnl 1s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_1s for fill-based version.
    formula: '((vol_1s - vol) * ((vega_1s + vega) / 2) - vol_path_estimate_1s) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_1s_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 1 second horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 1 second after, based on the
      fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 1s_fired_at
    - fire other slip 1s
    - fire other pnl 1s
    - fire unexplained pnl 1s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_1s for fill-based version.
    formula: '(tv_change_buysell_1s * contract_size * routed_size_capped) - (delta_slippage_1s_fired_at + roll_slippage_1s_fired_at)'
  - name: delta_slippage_1s_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 1 second horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 1 second after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 1s
    - fire delta slip 1s
    - fire delta pnl 1s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_1s for fill-based version.
    formula: '((calculated_base_val_1s - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1s_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 1 second horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 1 second after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 1s
    - fire roll slip 1s
    - fire roll pnl 1s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_1s for fill-based version.
    formula: '((reference_roll_1s - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_5s
    type: FLOAT
    description: Estimated volatility path effect at the 5 seconds horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 5 seconds after.
    synonyms: []
    formula: '((calculated_base_val_5s - swing_mid_base_val) * ((delta_5s - raw_delta_mid_bv_5s) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_5s_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 5 seconds horizon, measuring how much of the
      per-contract PnL change from trade time to 5 seconds after was attributable to implied volatility change. Expressed
      in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 5s
    - vol slip 5s per unit
    formula: '((vol_5s - vol) * ((vega_5s + vega) / 2) - vol_path_estimate_5s) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_5s
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 5 seconds horizon, measuring the total vol-driven
      PnL change from trade time to 5 seconds after. Equals vol_slippage_5s_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 5s
    formula: '((vol_5s - vol) * ((vega_5s + vega) / 2) - vol_path_estimate_5s) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_5s_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 5 seconds horizon, measuring how much of
      the per-contract PnL change from trade time to 5 seconds after was attributable to residual factors not captured by
      delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 5s
    - other slip 5s per unit
    formula: '(tv_change_buysell_5s * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_5s_per_unit + roll_slippage_5s_per_unit)'
  - name: other_slippage_5s
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 5 seconds horizon, measuring the total other-driven
      PnL change from trade time to 5 seconds after. Equals other_slippage_5s_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 5s
    formula: '(tv_change_buysell_5s * contract_size * trade_volume) - (delta_slippage_5s + roll_slippage_5s)'
  - name: tv_5s
    type: FLOAT
    description: Theoretical value (fair value) at 5 seconds after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 5s
  - name: delta_5s
    type: FLOAT
    description: Option delta at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 5s
  - name: gamma_5s
    type: FLOAT
    description: Option gamma at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_5s
    type: FLOAT
    description: Option vega at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_5s
    type: FLOAT
    description: Option theta at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_5s
    type: FLOAT
    description: Underlying price at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 5s
  - name: vol_5s
    type: FLOAT
    description: Implied volatility at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 5s
  - name: rho_5s
    type: FLOAT
    description: Option rho at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_5s
    type: FLOAT
    description: Raw (unadjusted) delta at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_5s
    type: FLOAT
    description: Raw (unadjusted) gamma at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_5s
    type: FLOAT
    description: Normalized strike (moneyness) at 5 seconds after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_5s
    type: FLOAT
    description: Roll adjustment value at 5 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_5s
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 5 seconds after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 5s
    formula: '(adjusted_tv_5s - swing_mid_tv)'
  - name: tv_change_buysell_5s
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 5 seconds, representing the realized PnL per unit from the TV
      movement. Positive = profitable direction.
    synonyms:
    - pnl 5s
    formula: '(adjusted_tv_5s - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_5s_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 5 seconds horizon, measuring how much of the per-contract
      PnL change from trade time to 5 seconds after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 5s
    - roll slip 5s per unit
    formula: '((reference_roll_5s - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_5s
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 5 seconds horizon, measuring the total roll-driven PnL
      change from trade time to 5 seconds after. Equals roll_slippage_5s_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 5s
    formula: '((reference_roll_5s - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_5s
    type: FLOAT
    description: Adjusted theoretical value at 5 seconds after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_5s + delta_5s * (calculated_base_val_5s - (under_5s - roll_5s)) + 0.5 * gamma_5s * POW((calculated_base_val_5s - (under_5s - roll_5s)), 2)'
  - name: delta_slippage_5s_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 5 seconds horizon, measuring how much of the per-contract
      PnL change from trade time to 5 seconds after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 5s
    - delta slip 5s per unit
    formula: '((calculated_base_val_5s - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_5s
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 5 seconds horizon, measuring the total delta-driven
      PnL change from trade time to 5 seconds after. Equals delta_slippage_5s_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 5s
    formula: '((calculated_base_val_5s - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_5s_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 5 seconds horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the volatility change contribution to PnL between trade time and 5 seconds after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 5s
    - fire vol slip 5s
    - fire vol pnl 5s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_5s for fill-based version.
    formula: '((vol_5s - vol) * ((vega_5s + vega) / 2) - vol_path_estimate_5s) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_5s_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 5 seconds horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 5 seconds after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 5s_fired_at
    - fire other slip 5s
    - fire other pnl 5s
    - fire unexplained pnl 5s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_5s for fill-based version.
    formula: '(tv_change_buysell_5s * contract_size * routed_size_capped) - (delta_slippage_5s_fired_at + roll_slippage_5s_fired_at)'
  - name: delta_slippage_5s_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 5 seconds horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 5 seconds after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 5s
    - fire delta slip 5s
    - fire delta pnl 5s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_5s for fill-based version.
    formula: '((calculated_base_val_5s - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_5s_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 5 seconds horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 5 seconds after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 5s
    - fire roll slip 5s
    - fire roll pnl 5s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_5s for fill-based version.
    formula: '((reference_roll_5s - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_10s
    type: FLOAT
    description: Estimated volatility path effect at the 10 seconds horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 10 seconds after.
    synonyms: []
    formula: '((calculated_base_val_10s - swing_mid_base_val) * ((delta_10s - raw_delta_mid_bv_10s) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_10s_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 10 seconds horizon, measuring how much of the
      per-contract PnL change from trade time to 10 seconds after was attributable to implied volatility change. Expressed
      in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 10s
    - vol slip 10s per unit
    formula: '((vol_10s - vol) * ((vega_10s + vega) / 2) - vol_path_estimate_10s) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_10s
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 10 seconds horizon, measuring the total vol-driven
      PnL change from trade time to 10 seconds after. Equals vol_slippage_10s_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 10s
    formula: '((vol_10s - vol) * ((vega_10s + vega) / 2) - vol_path_estimate_10s) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_10s_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 10 seconds horizon, measuring how much of
      the per-contract PnL change from trade time to 10 seconds after was attributable to residual factors not captured by
      delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 10s
    - other slip 10s per unit
    formula: '(tv_change_buysell_10s * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_10s_per_unit + roll_slippage_10s_per_unit)'
  - name: other_slippage_10s
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 10 seconds horizon, measuring the total other-driven
      PnL change from trade time to 10 seconds after. Equals other_slippage_10s_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 10s
    formula: '(tv_change_buysell_10s * contract_size * trade_volume) - (delta_slippage_10s + roll_slippage_10s)'
  - name: tv_10s
    type: FLOAT
    description: Theoretical value (fair value) at 10 seconds after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 10s
  - name: delta_10s
    type: FLOAT
    description: Option delta at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 10s
  - name: gamma_10s
    type: FLOAT
    description: Option gamma at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_10s
    type: FLOAT
    description: Option vega at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_10s
    type: FLOAT
    description: Option theta at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_10s
    type: FLOAT
    description: Underlying price at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 10s
  - name: vol_10s
    type: FLOAT
    description: Implied volatility at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 10s
  - name: rho_10s
    type: FLOAT
    description: Option rho at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_10s
    type: FLOAT
    description: Raw (unadjusted) delta at 10 seconds after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_10s
    type: FLOAT
    description: Raw (unadjusted) gamma at 10 seconds after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_10s
    type: FLOAT
    description: Normalized strike (moneyness) at 10 seconds after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_10s
    type: FLOAT
    description: Roll adjustment value at 10 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_10s
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 10 seconds after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 10s
    formula: '(adjusted_tv_10s - swing_mid_tv)'
  - name: tv_change_buysell_10s
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 10 seconds, representing the realized PnL per unit from the
      TV movement. Positive = profitable direction.
    synonyms:
    - pnl 10s
    formula: '(adjusted_tv_10s - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_10s_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 10 seconds horizon, measuring how much of the per-contract
      PnL change from trade time to 10 seconds after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 10s
    - roll slip 10s per unit
    formula: '((reference_roll_10s - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_10s
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 10 seconds horizon, measuring the total roll-driven PnL
      change from trade time to 10 seconds after. Equals roll_slippage_10s_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 10s
    formula: '((reference_roll_10s - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_10s
    type: FLOAT
    description: Adjusted theoretical value at 10 seconds after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_10s + delta_10s * (calculated_base_val_10s - (under_10s - roll_10s)) + 0.5 * gamma_10s * POW((calculated_base_val_10s - (under_10s - roll_10s)), 2)'
  - name: delta_slippage_10s_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 10 seconds horizon, measuring how much of the per-contract
      PnL change from trade time to 10 seconds after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 10s
    - delta slip 10s per unit
    formula: '((calculated_base_val_10s - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_10s
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 10 seconds horizon, measuring the total delta-driven
      PnL change from trade time to 10 seconds after. Equals delta_slippage_10s_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 10s
    formula: '((calculated_base_val_10s - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_10s_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 10 seconds horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the volatility change contribution to PnL between trade time and 10 seconds after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 10s
    - fire vol slip 10s
    - fire vol pnl 10s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_10s for fill-based version.
    formula: '((vol_10s - vol) * ((vega_10s + vega) / 2) - vol_path_estimate_10s) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_10s_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 10 seconds horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 10 seconds after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 10s_fired_at
    - fire other slip 10s
    - fire other pnl 10s
    - fire unexplained pnl 10s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_10s for fill-based version.
    formula: '(tv_change_buysell_10s * contract_size * routed_size_capped) - (delta_slippage_10s_fired_at + roll_slippage_10s_fired_at)'
  - name: delta_slippage_10s_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 10 seconds horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 10 seconds after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 10s
    - fire delta slip 10s
    - fire delta pnl 10s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_10s for fill-based version.
    formula: '((calculated_base_val_10s - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_10s_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 10 seconds horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 10 seconds after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 10s
    - fire roll slip 10s
    - fire roll pnl 10s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_10s for fill-based version.
    formula: '((reference_roll_10s - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_30s
    type: FLOAT
    description: Estimated volatility path effect at the 30 seconds horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 30 seconds after.
    synonyms: []
    formula: '((calculated_base_val_30s - swing_mid_base_val) * ((delta_30s - raw_delta_mid_bv_30s) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_30s_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 30 seconds horizon, measuring how much of the
      per-contract PnL change from trade time to 30 seconds after was attributable to implied volatility change. Expressed
      in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 30s
    - vol slip 30s per unit
    formula: '((vol_30s - vol) * ((vega_30s + vega) / 2) - vol_path_estimate_30s) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_30s
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 30 seconds horizon, measuring the total vol-driven
      PnL change from trade time to 30 seconds after. Equals vol_slippage_30s_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 30s
    formula: '((vol_30s - vol) * ((vega_30s + vega) / 2) - vol_path_estimate_30s) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_30s_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 30 seconds horizon, measuring how much of
      the per-contract PnL change from trade time to 30 seconds after was attributable to residual factors not captured by
      delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 30s
    - other slip 30s per unit
    formula: '(tv_change_buysell_30s * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_30s_per_unit + roll_slippage_30s_per_unit)'
  - name: other_slippage_30s
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 30 seconds horizon, measuring the total other-driven
      PnL change from trade time to 30 seconds after. Equals other_slippage_30s_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 30s
    formula: '(tv_change_buysell_30s * contract_size * trade_volume) - (delta_slippage_30s + roll_slippage_30s)'
  - name: tv_30s
    type: FLOAT
    description: Theoretical value (fair value) at 30 seconds after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 30s
  - name: delta_30s
    type: FLOAT
    description: Option delta at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 30s
  - name: gamma_30s
    type: FLOAT
    description: Option gamma at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_30s
    type: FLOAT
    description: Option vega at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_30s
    type: FLOAT
    description: Option theta at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_30s
    type: FLOAT
    description: Underlying price at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 30s
  - name: vol_30s
    type: FLOAT
    description: Implied volatility at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 30s
  - name: rho_30s
    type: FLOAT
    description: Option rho at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_30s
    type: FLOAT
    description: Raw (unadjusted) delta at 30 seconds after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_30s
    type: FLOAT
    description: Raw (unadjusted) gamma at 30 seconds after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_30s
    type: FLOAT
    description: Normalized strike (moneyness) at 30 seconds after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_30s
    type: FLOAT
    description: Roll adjustment value at 30 seconds after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_30s
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 30 seconds after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 30s
    formula: '(adjusted_tv_30s - swing_mid_tv)'
  - name: tv_change_buysell_30s
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 30 seconds, representing the realized PnL per unit from the
      TV movement. Positive = profitable direction.
    synonyms:
    - pnl 30s
    formula: '(adjusted_tv_30s - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_30s_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 30 seconds horizon, measuring how much of the per-contract
      PnL change from trade time to 30 seconds after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 30s
    - roll slip 30s per unit
    formula: '((reference_roll_30s - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_30s
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 30 seconds horizon, measuring the total roll-driven PnL
      change from trade time to 30 seconds after. Equals roll_slippage_30s_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 30s
    formula: '((reference_roll_30s - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_30s
    type: FLOAT
    description: Adjusted theoretical value at 30 seconds after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_30s + delta_30s * (calculated_base_val_30s - (under_30s - roll_30s)) + 0.5 * gamma_30s * POW((calculated_base_val_30s - (under_30s - roll_30s)), 2)'
  - name: delta_slippage_30s_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 30 seconds horizon, measuring how much of the per-contract
      PnL change from trade time to 30 seconds after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 30s
    - delta slip 30s per unit
    formula: '((calculated_base_val_30s - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_30s
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 30 seconds horizon, measuring the total delta-driven
      PnL change from trade time to 30 seconds after. Equals delta_slippage_30s_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 30s
    formula: '((calculated_base_val_30s - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_30s_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 30 seconds horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the volatility change contribution to PnL between trade time and 30 seconds after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 30s
    - fire vol slip 30s
    - fire vol pnl 30s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_30s for fill-based version.
    formula: '((vol_30s - vol) * ((vega_30s + vega) / 2) - vol_path_estimate_30s) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_30s_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 30 seconds horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 30 seconds after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 30s_fired_at
    - fire other slip 30s
    - fire other pnl 30s
    - fire unexplained pnl 30s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_30s for fill-based version.
    formula: '(tv_change_buysell_30s * contract_size * routed_size_capped) - (delta_slippage_30s_fired_at + roll_slippage_30s_fired_at)'
  - name: delta_slippage_30s_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 30 seconds horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 30 seconds after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 30s
    - fire delta slip 30s
    - fire delta pnl 30s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_30s for fill-based version.
    formula: '((calculated_base_val_30s - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_30s_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 30 seconds horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 30 seconds after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 30s
    - fire roll slip 30s
    - fire roll pnl 30s
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_30s for fill-based version.
    formula: '((reference_roll_30s - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_1m
    type: FLOAT
    description: Estimated volatility path effect at the 1 minute horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 1 minute after.
    synonyms: []
    formula: '((calculated_base_val_1m - swing_mid_base_val) * ((delta_1m - raw_delta_mid_bv_1m) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_1m_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 1 minute horizon, measuring how much of the
      per-contract PnL change from trade time to 1 minute after was attributable to implied volatility change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 1m
    - vol slip 1m per unit
    formula: '((vol_1m - vol) * ((vega_1m + vega) / 2) - vol_path_estimate_1m) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1m
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 1 minute horizon, measuring the total vol-driven
      PnL change from trade time to 1 minute after. Equals vol_slippage_1m_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 1m
    formula: '((vol_1m - vol) * ((vega_1m + vega) / 2) - vol_path_estimate_1m) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_1m_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 1 minute horizon, measuring how much of
      the per-contract PnL change from trade time to 1 minute after was attributable to residual factors not captured by delta,
      vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 1m
    - other slip 1m per unit
    formula: '(tv_change_buysell_1m * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_1m_per_unit + roll_slippage_1m_per_unit)'
  - name: other_slippage_1m
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 1 minute horizon, measuring the total other-driven
      PnL change from trade time to 1 minute after. Equals other_slippage_1m_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 1m
    formula: '(tv_change_buysell_1m * contract_size * trade_volume) - (delta_slippage_1m + roll_slippage_1m)'
  - name: tv_1m
    type: FLOAT
    description: Theoretical value (fair value) at 1 minute after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 1m
  - name: delta_1m
    type: FLOAT
    description: Option delta at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 1m
  - name: gamma_1m
    type: FLOAT
    description: Option gamma at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_1m
    type: FLOAT
    description: Option vega at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_1m
    type: FLOAT
    description: Option theta at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_1m
    type: FLOAT
    description: Underlying price at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 1m
  - name: vol_1m
    type: FLOAT
    description: Implied volatility at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 1m
  - name: rho_1m
    type: FLOAT
    description: Option rho at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_1m
    type: FLOAT
    description: Raw (unadjusted) delta at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_1m
    type: FLOAT
    description: Raw (unadjusted) gamma at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_1m
    type: FLOAT
    description: Normalized strike (moneyness) at 1 minute after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_1m
    type: FLOAT
    description: Roll adjustment value at 1 minute after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_1m
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 1 minute after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 1m
    formula: '(adjusted_tv_1m - swing_mid_tv)'
  - name: tv_change_buysell_1m
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 1 minute, representing the realized PnL per unit from the TV
      movement. Positive = profitable direction.
    synonyms:
    - pnl 1m
    formula: '(adjusted_tv_1m - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_1m_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 1 minute horizon, measuring how much of the per-contract
      PnL change from trade time to 1 minute after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 1m
    - roll slip 1m per unit
    formula: '((reference_roll_1m - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1m
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 1 minute horizon, measuring the total roll-driven PnL
      change from trade time to 1 minute after. Equals roll_slippage_1m_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 1m
    formula: '((reference_roll_1m - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_1m
    type: FLOAT
    description: Adjusted theoretical value at 1 minute after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_1m + delta_1m * (calculated_base_val_1m - (under_1m - roll_1m)) + 0.5 * gamma_1m * POW((calculated_base_val_1m - (under_1m - roll_1m)), 2)'
  - name: delta_slippage_1m_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 1 minute horizon, measuring how much of the per-contract
      PnL change from trade time to 1 minute after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 1m
    - delta slip 1m per unit
    formula: '((calculated_base_val_1m - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_1m
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 1 minute horizon, measuring the total delta-driven PnL
      change from trade time to 1 minute after. Equals delta_slippage_1m_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 1m
    formula: '((calculated_base_val_1m - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1m_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 1 minute horizon, computed using routed_size (fired size) instead of
      trade_volume. Measures the volatility change contribution to PnL between trade time and 1 minute after, based on the
      fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 1m
    - fire vol slip 1m
    - fire vol pnl 1m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_1m for fill-based version.
    formula: '((vol_1m - vol) * ((vega_1m + vega) / 2) - vol_path_estimate_1m) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_1m_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 1 minute horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 1 minute after, based on the
      fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 1m_fired_at
    - fire other slip 1m
    - fire other pnl 1m
    - fire unexplained pnl 1m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_1m for fill-based version.
    formula: '(tv_change_buysell_1m * contract_size * routed_size_capped) - (delta_slippage_1m_fired_at + roll_slippage_1m_fired_at)'
  - name: delta_slippage_1m_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 1 minute horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 1 minute after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 1m
    - fire delta slip 1m
    - fire delta pnl 1m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_1m for fill-based version.
    formula: '((calculated_base_val_1m - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1m_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 1 minute horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 1 minute after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 1m
    - fire roll slip 1m
    - fire roll pnl 1m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_1m for fill-based version.
    formula: '((reference_roll_1m - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_5m
    type: FLOAT
    description: Estimated volatility path effect at the 5 minutes horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 5 minutes after.
    synonyms: []
    formula: '((calculated_base_val_5m - swing_mid_base_val) * ((delta_5m - raw_delta_mid_bv_5m) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_5m_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 5 minutes horizon, measuring how much of the
      per-contract PnL change from trade time to 5 minutes after was attributable to implied volatility change. Expressed
      in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 5m
    - vol slip 5m per unit
    formula: '((vol_5m - vol) * ((vega_5m + vega) / 2) - vol_path_estimate_5m) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_5m
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 5 minutes horizon, measuring the total vol-driven
      PnL change from trade time to 5 minutes after. Equals vol_slippage_5m_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 5m
    formula: '((vol_5m - vol) * ((vega_5m + vega) / 2) - vol_path_estimate_5m) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_5m_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 5 minutes horizon, measuring how much of
      the per-contract PnL change from trade time to 5 minutes after was attributable to residual factors not captured by
      delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 5m
    - other slip 5m per unit
    formula: '(tv_change_buysell_5m * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_5m_per_unit + roll_slippage_5m_per_unit)'
  - name: other_slippage_5m
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 5 minutes horizon, measuring the total other-driven
      PnL change from trade time to 5 minutes after. Equals other_slippage_5m_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 5m
    formula: '(tv_change_buysell_5m * contract_size * trade_volume) - (delta_slippage_5m + roll_slippage_5m)'
  - name: tv_5m
    type: FLOAT
    description: Theoretical value (fair value) at 5 minutes after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 5m
  - name: delta_5m
    type: FLOAT
    description: Option delta at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 5m
  - name: gamma_5m
    type: FLOAT
    description: Option gamma at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_5m
    type: FLOAT
    description: Option vega at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_5m
    type: FLOAT
    description: Option theta at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_5m
    type: FLOAT
    description: Underlying price at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 5m
  - name: vol_5m
    type: FLOAT
    description: Implied volatility at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 5m
  - name: rho_5m
    type: FLOAT
    description: Option rho at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_5m
    type: FLOAT
    description: Raw (unadjusted) delta at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_5m
    type: FLOAT
    description: Raw (unadjusted) gamma at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_5m
    type: FLOAT
    description: Normalized strike (moneyness) at 5 minutes after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_5m
    type: FLOAT
    description: Roll adjustment value at 5 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_5m
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 5 minutes after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 5m
    formula: '(adjusted_tv_5m - swing_mid_tv)'
  - name: tv_change_buysell_5m
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 5 minutes, representing the realized PnL per unit from the TV
      movement. Positive = profitable direction.
    synonyms:
    - pnl 5m
    formula: '(adjusted_tv_5m - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_5m_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 5 minutes horizon, measuring how much of the per-contract
      PnL change from trade time to 5 minutes after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 5m
    - roll slip 5m per unit
    formula: '((reference_roll_5m - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_5m
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 5 minutes horizon, measuring the total roll-driven PnL
      change from trade time to 5 minutes after. Equals roll_slippage_5m_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 5m
    formula: '((reference_roll_5m - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_5m
    type: FLOAT
    description: Adjusted theoretical value at 5 minutes after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_5m + delta_5m * (calculated_base_val_5m - (under_5m - roll_5m)) + 0.5 * gamma_5m * POW((calculated_base_val_5m - (under_5m - roll_5m)), 2)'
  - name: delta_slippage_5m_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 5 minutes horizon, measuring how much of the per-contract
      PnL change from trade time to 5 minutes after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 5m
    - delta slip 5m per unit
    formula: '((calculated_base_val_5m - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_5m
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 5 minutes horizon, measuring the total delta-driven
      PnL change from trade time to 5 minutes after. Equals delta_slippage_5m_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 5m
    formula: '((calculated_base_val_5m - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_5m_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 5 minutes horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the volatility change contribution to PnL between trade time and 5 minutes after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 5m
    - fire vol slip 5m
    - fire vol pnl 5m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_5m for fill-based version.
    formula: '((vol_5m - vol) * ((vega_5m + vega) / 2) - vol_path_estimate_5m) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_5m_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 5 minutes horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 5 minutes after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 5m_fired_at
    - fire other slip 5m
    - fire other pnl 5m
    - fire unexplained pnl 5m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_5m for fill-based version.
    formula: '(tv_change_buysell_5m * contract_size * routed_size_capped) - (delta_slippage_5m_fired_at + roll_slippage_5m_fired_at)'
  - name: delta_slippage_5m_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 5 minutes horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 5 minutes after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 5m
    - fire delta slip 5m
    - fire delta pnl 5m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_5m for fill-based version.
    formula: '((calculated_base_val_5m - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_5m_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 5 minutes horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 5 minutes after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 5m
    - fire roll slip 5m
    - fire roll pnl 5m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_5m for fill-based version.
    formula: '((reference_roll_5m - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_10m
    type: FLOAT
    description: Estimated volatility path effect at the 10 minutes horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 10 minutes after.
    synonyms: []
    formula: '((calculated_base_val_10m - swing_mid_base_val) * ((delta_10m - raw_delta_mid_bv_10m) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_10m_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 10 minutes horizon, measuring how much of the
      per-contract PnL change from trade time to 10 minutes after was attributable to implied volatility change. Expressed
      in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 10m
    - vol slip 10m per unit
    formula: '((vol_10m - vol) * ((vega_10m + vega) / 2) - vol_path_estimate_10m) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_10m
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 10 minutes horizon, measuring the total vol-driven
      PnL change from trade time to 10 minutes after. Equals vol_slippage_10m_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 10m
    formula: '((vol_10m - vol) * ((vega_10m + vega) / 2) - vol_path_estimate_10m) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_10m_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 10 minutes horizon, measuring how much of
      the per-contract PnL change from trade time to 10 minutes after was attributable to residual factors not captured by
      delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 10m
    - other slip 10m per unit
    formula: '(tv_change_buysell_10m * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_10m_per_unit + roll_slippage_10m_per_unit)'
  - name: other_slippage_10m
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 10 minutes horizon, measuring the total other-driven
      PnL change from trade time to 10 minutes after. Equals other_slippage_10m_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 10m
    formula: '(tv_change_buysell_10m * contract_size * trade_volume) - (delta_slippage_10m + roll_slippage_10m)'
  - name: tv_10m
    type: FLOAT
    description: Theoretical value (fair value) at 10 minutes after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 10m
  - name: delta_10m
    type: FLOAT
    description: Option delta at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 10m
  - name: gamma_10m
    type: FLOAT
    description: Option gamma at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_10m
    type: FLOAT
    description: Option vega at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_10m
    type: FLOAT
    description: Option theta at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_10m
    type: FLOAT
    description: Underlying price at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 10m
  - name: vol_10m
    type: FLOAT
    description: Implied volatility at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 10m
  - name: rho_10m
    type: FLOAT
    description: Option rho at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_10m
    type: FLOAT
    description: Raw (unadjusted) delta at 10 minutes after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_10m
    type: FLOAT
    description: Raw (unadjusted) gamma at 10 minutes after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_10m
    type: FLOAT
    description: Normalized strike (moneyness) at 10 minutes after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_10m
    type: FLOAT
    description: Roll adjustment value at 10 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_10m
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 10 minutes after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 10m
    formula: '(adjusted_tv_10m - swing_mid_tv)'
  - name: tv_change_buysell_10m
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 10 minutes, representing the realized PnL per unit from the
      TV movement. Positive = profitable direction.
    synonyms:
    - pnl 10m
    formula: '(adjusted_tv_10m - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_10m_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 10 minutes horizon, measuring how much of the per-contract
      PnL change from trade time to 10 minutes after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 10m
    - roll slip 10m per unit
    formula: '((reference_roll_10m - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_10m
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 10 minutes horizon, measuring the total roll-driven PnL
      change from trade time to 10 minutes after. Equals roll_slippage_10m_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 10m
    formula: '((reference_roll_10m - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_10m
    type: FLOAT
    description: Adjusted theoretical value at 10 minutes after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_10m + delta_10m * (calculated_base_val_10m - (under_10m - roll_10m)) + 0.5 * gamma_10m * POW((calculated_base_val_10m - (under_10m - roll_10m)), 2)'
  - name: delta_slippage_10m_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 10 minutes horizon, measuring how much of the per-contract
      PnL change from trade time to 10 minutes after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 10m
    - delta slip 10m per unit
    formula: '((calculated_base_val_10m - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_10m
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 10 minutes horizon, measuring the total delta-driven
      PnL change from trade time to 10 minutes after. Equals delta_slippage_10m_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 10m
    formula: '((calculated_base_val_10m - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_10m_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 10 minutes horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the volatility change contribution to PnL between trade time and 10 minutes after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 10m
    - fire vol slip 10m
    - fire vol pnl 10m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_10m for fill-based version.
    formula: '((vol_10m - vol) * ((vega_10m + vega) / 2) - vol_path_estimate_10m) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_10m_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 10 minutes horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 10 minutes after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 10m_fired_at
    - fire other slip 10m
    - fire other pnl 10m
    - fire unexplained pnl 10m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_10m for fill-based version.
    formula: '(tv_change_buysell_10m * contract_size * routed_size_capped) - (delta_slippage_10m_fired_at + roll_slippage_10m_fired_at)'
  - name: delta_slippage_10m_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 10 minutes horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 10 minutes after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 10m
    - fire delta slip 10m
    - fire delta pnl 10m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_10m for fill-based version.
    formula: '((calculated_base_val_10m - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_10m_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 10 minutes horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 10 minutes after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 10m
    - fire roll slip 10m
    - fire roll pnl 10m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_10m for fill-based version.
    formula: '((reference_roll_10m - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_30m
    type: FLOAT
    description: Estimated volatility path effect at the 30 minutes horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 30 minutes after.
    synonyms: []
    formula: '((calculated_base_val_30m - swing_mid_base_val) * ((delta_30m - raw_delta_mid_bv_30m) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_30m_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 30 minutes horizon, measuring how much of the
      per-contract PnL change from trade time to 30 minutes after was attributable to implied volatility change. Expressed
      in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 30m
    - vol slip 30m per unit
    formula: '((vol_30m - vol) * ((vega_30m + vega) / 2) - vol_path_estimate_30m) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_30m
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 30 minutes horizon, measuring the total vol-driven
      PnL change from trade time to 30 minutes after. Equals vol_slippage_30m_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 30m
    formula: '((vol_30m - vol) * ((vega_30m + vega) / 2) - vol_path_estimate_30m) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_30m_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 30 minutes horizon, measuring how much of
      the per-contract PnL change from trade time to 30 minutes after was attributable to residual factors not captured by
      delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 30m
    - other slip 30m per unit
    formula: '(tv_change_buysell_30m * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_30m_per_unit + roll_slippage_30m_per_unit)'
  - name: other_slippage_30m
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 30 minutes horizon, measuring the total other-driven
      PnL change from trade time to 30 minutes after. Equals other_slippage_30m_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 30m
    formula: '(tv_change_buysell_30m * contract_size * trade_volume) - (delta_slippage_30m + roll_slippage_30m)'
  - name: tv_30m
    type: FLOAT
    description: Theoretical value (fair value) at 30 minutes after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 30m
  - name: delta_30m
    type: FLOAT
    description: Option delta at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 30m
  - name: gamma_30m
    type: FLOAT
    description: Option gamma at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_30m
    type: FLOAT
    description: Option vega at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_30m
    type: FLOAT
    description: Option theta at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_30m
    type: FLOAT
    description: Underlying price at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 30m
  - name: vol_30m
    type: FLOAT
    description: Implied volatility at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 30m
  - name: rho_30m
    type: FLOAT
    description: Option rho at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_30m
    type: FLOAT
    description: Raw (unadjusted) delta at 30 minutes after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_30m
    type: FLOAT
    description: Raw (unadjusted) gamma at 30 minutes after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_30m
    type: FLOAT
    description: Normalized strike (moneyness) at 30 minutes after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_30m
    type: FLOAT
    description: Roll adjustment value at 30 minutes after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_30m
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 30 minutes after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 30m
    formula: '(adjusted_tv_30m - swing_mid_tv)'
  - name: tv_change_buysell_30m
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 30 minutes, representing the realized PnL per unit from the
      TV movement. Positive = profitable direction.
    synonyms:
    - pnl 30m
    formula: '(adjusted_tv_30m - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_30m_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 30 minutes horizon, measuring how much of the per-contract
      PnL change from trade time to 30 minutes after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 30m
    - roll slip 30m per unit
    formula: '((reference_roll_30m - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_30m
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 30 minutes horizon, measuring the total roll-driven PnL
      change from trade time to 30 minutes after. Equals roll_slippage_30m_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 30m
    formula: '((reference_roll_30m - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_30m
    type: FLOAT
    description: Adjusted theoretical value at 30 minutes after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_30m + delta_30m * (calculated_base_val_30m - (under_30m - roll_30m)) + 0.5 * gamma_30m * POW((calculated_base_val_30m - (under_30m - roll_30m)), 2)'
  - name: delta_slippage_30m_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 30 minutes horizon, measuring how much of the per-contract
      PnL change from trade time to 30 minutes after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 30m
    - delta slip 30m per unit
    formula: '((calculated_base_val_30m - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_30m
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 30 minutes horizon, measuring the total delta-driven
      PnL change from trade time to 30 minutes after. Equals delta_slippage_30m_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 30m
    formula: '((calculated_base_val_30m - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_30m_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 30 minutes horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the volatility change contribution to PnL between trade time and 30 minutes after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 30m
    - fire vol slip 30m
    - fire vol pnl 30m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_30m for fill-based version.
    formula: '((vol_30m - vol) * ((vega_30m + vega) / 2) - vol_path_estimate_30m) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_30m_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 30 minutes horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 30 minutes after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 30m_fired_at
    - fire other slip 30m
    - fire other pnl 30m
    - fire unexplained pnl 30m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_30m for fill-based version.
    formula: '(tv_change_buysell_30m * contract_size * routed_size_capped) - (delta_slippage_30m_fired_at + roll_slippage_30m_fired_at)'
  - name: delta_slippage_30m_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 30 minutes horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 30 minutes after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 30m
    - fire delta slip 30m
    - fire delta pnl 30m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_30m for fill-based version.
    formula: '((calculated_base_val_30m - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_30m_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 30 minutes horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 30 minutes after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 30m
    - fire roll slip 30m
    - fire roll pnl 30m
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_30m for fill-based version.
    formula: '((reference_roll_30m - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_1h
    type: FLOAT
    description: Estimated volatility path effect at the 1 hour horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and 1 hour after.
    synonyms: []
    formula: '((calculated_base_val_1h - swing_mid_base_val) * ((delta_1h - raw_delta_mid_bv_1h) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_1h_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the 1 hour horizon, measuring how much of the per-contract
      PnL change from trade time to 1 hour after was attributable to implied volatility change. Expressed in ticks. Multiply
      by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 1h
    - vol slip 1h per unit
    formula: '((vol_1h - vol) * ((vega_1h + vega) / 2) - vol_path_estimate_1h) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1h
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the 1 hour horizon, measuring the total vol-driven
      PnL change from trade time to 1 hour after. Equals vol_slippage_1h_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 1h
    formula: '((vol_1h - vol) * ((vega_1h + vega) / 2) - vol_path_estimate_1h) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_1h_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the 1 hour horizon, measuring how much of the
      per-contract PnL change from trade time to 1 hour after was attributable to residual factors not captured by delta,
      vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip 1h
    - other slip 1h per unit
    formula: '(tv_change_buysell_1h * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_1h_per_unit + roll_slippage_1h_per_unit)'
  - name: other_slippage_1h
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the 1 hour horizon, measuring the total other-driven
      PnL change from trade time to 1 hour after. Equals other_slippage_1h_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip 1h
    formula: '(tv_change_buysell_1h * contract_size * trade_volume) - (delta_slippage_1h + roll_slippage_1h)'
  - name: tv_1h
    type: FLOAT
    description: Theoretical value (fair value) at 1 hour after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 1h
  - name: delta_1h
    type: FLOAT
    description: Option delta at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 1h
  - name: gamma_1h
    type: FLOAT
    description: Option gamma at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_1h
    type: FLOAT
    description: Option vega at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_1h
    type: FLOAT
    description: Option theta at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_1h
    type: FLOAT
    description: Underlying price at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 1h
  - name: vol_1h
    type: FLOAT
    description: Implied volatility at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 1h
  - name: rho_1h
    type: FLOAT
    description: Option rho at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot. Used to
      compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_1h
    type: FLOAT
    description: Raw (unadjusted) delta at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_1h
    type: FLOAT
    description: Raw (unadjusted) gamma at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_1h
    type: FLOAT
    description: Normalized strike (moneyness) at 1 hour after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_1h
    type: FLOAT
    description: Roll adjustment value at 1 hour after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_1h
    type: FLOAT
    description: Absolute change in theoretical value between trade time and 1 hour after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move 1h
    formula: '(adjusted_tv_1h - swing_mid_tv)'
  - name: tv_change_buysell_1h
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at 1 hour, representing the realized PnL per unit from the TV movement.
      Positive = profitable direction.
    synonyms:
    - pnl 1h
    formula: '(adjusted_tv_1h - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_1h_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the 1 hour horizon, measuring how much of the per-contract
      PnL change from trade time to 1 hour after was attributable to cost-of-carry (roll/dividend) change. Expressed in ticks.
      Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 1h
    - roll slip 1h per unit
    formula: '((reference_roll_1h - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1h
    type: FLOAT
    description: Roll slippage component (total, in currency) at the 1 hour horizon, measuring the total roll-driven PnL change
      from trade time to 1 hour after. Equals roll_slippage_1h_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 1h
    formula: '((reference_roll_1h - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_1h
    type: FLOAT
    description: Adjusted theoretical value at 1 hour after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_1h + delta_1h * (calculated_base_val_1h - (under_1h - roll_1h)) + 0.5 * gamma_1h * POW((calculated_base_val_1h - (under_1h - roll_1h)), 2)'
  - name: delta_slippage_1h_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the 1 hour horizon, measuring how much of the per-contract
      PnL change from trade time to 1 hour after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 1h
    - delta slip 1h per unit
    formula: '((calculated_base_val_1h - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_1h
    type: FLOAT
    description: Delta slippage component (total, in currency) at the 1 hour horizon, measuring the total delta-driven PnL
      change from trade time to 1 hour after. Equals delta_slippage_1h_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 1h
    formula: '((calculated_base_val_1h - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1h_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the 1 hour horizon, computed using routed_size (fired size) instead of
      trade_volume. Measures the volatility change contribution to PnL between trade time and 1 hour after, based on the fired
      quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 1h
    - fire vol slip 1h
    - fire vol pnl 1h
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_1h for fill-based version.
    formula: '((vol_1h - vol) * ((vega_1h + vega) / 2) - vol_path_estimate_1h) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_1h_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the 1 hour horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and 1 hour after, based on the
      fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 1h_fired_at
    - fire other slip 1h
    - fire other pnl 1h
    - fire unexplained pnl 1h
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_1h for fill-based version.
    formula: '(tv_change_buysell_1h * contract_size * routed_size_capped) - (delta_slippage_1h_fired_at + roll_slippage_1h_fired_at)'
  - name: delta_slippage_1h_fired_at
    type: FLOAT
    description: 'Delta slippage component at the 1 hour horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and 1 hour after, based on the fired quantity. Unique
      to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 1h
    - fire delta slip 1h
    - fire delta pnl 1h
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_1h for fill-based version.
    formula: '((calculated_base_val_1h - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1h_fired_at
    type: FLOAT
    description: 'Roll slippage component at the 1 hour horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and 1 hour after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 1h
    - fire roll slip 1h
    - fire roll pnl 1h
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_1h for fill-based version.
    formula: '((reference_roll_1h - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_eod
    type: FLOAT
    description: Estimated volatility path effect at the end-of-day horizon, measuring the expected impact of implied volatility
      mean-reversion or trending on option value between trade time and end-of-day after.
    synonyms: []
    formula: '((calculated_base_val_eod - swing_mid_base_val) * ((delta_eod - raw_delta_mid_bv_eod) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_eod_per_unit
    type: FLOAT
    description: Volatility slippage component per unit (per contract) at the end-of-day horizon, measuring how much of the
      per-contract PnL change from trade time to end-of-day after was attributable to implied volatility change. Expressed
      in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip eod
    - vol slip eod per unit
    formula: '((vol_eod - vol) * ((vega_eod + vega) / 2) - vol_path_estimate_eod) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_eod
    type: FLOAT
    description: Volatility slippage component (total, in currency) at the end-of-day horizon, measuring the total vol-driven
      PnL change from trade time to end-of-day after. Equals vol_slippage_eod_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip eod
    formula: '((vol_eod - vol) * ((vega_eod + vega) / 2) - vol_path_estimate_eod) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_eod_per_unit
    type: FLOAT
    description: Residual/other slippage component per unit (per contract) at the end-of-day horizon, measuring how much of
      the per-contract PnL change from trade time to end-of-day after was attributable to residual factors not captured by
      delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other slippage in currency.
    synonyms:
    - other slip eod
    - other slip eod per unit
    formula: '(tv_change_buysell_eod * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_eod_per_unit + roll_slippage_eod_per_unit)'
  - name: other_slippage_eod
    type: FLOAT
    description: Residual/other slippage component (total, in currency) at the end-of-day horizon, measuring the total other-driven
      PnL change from trade time to end-of-day after. Equals other_slippage_eod_per_unit * trade_volume * contract_size.
    synonyms:
    - other slip eod
    formula: '(tv_change_buysell_eod * contract_size * trade_volume) - (delta_slippage_eod + roll_slippage_eod)'
  - name: tv_eod
    type: FLOAT
    description: Theoretical value (fair value) at end-of-day after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo eod
  - name: delta_eod
    type: FLOAT
    description: Option delta at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta eod
  - name: gamma_eod
    type: FLOAT
    description: Option gamma at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_eod
    type: FLOAT
    description: Option vega at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_eod
    type: FLOAT
    description: Option theta at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_eod
    type: FLOAT
    description: Underlying price at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot eod
  - name: vol_eod
    type: FLOAT
    description: Implied volatility at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV eod
  - name: rho_eod
    type: FLOAT
    description: Option rho at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot. Used
      to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_eod
    type: FLOAT
    description: Raw (unadjusted) delta at end-of-day after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_eod
    type: FLOAT
    description: Raw (unadjusted) gamma at end-of-day after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_eod
    type: FLOAT
    description: Normalized strike (moneyness) at end-of-day after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_eod
    type: FLOAT
    description: Roll adjustment value at end-of-day after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_eod
    type: FLOAT
    description: Absolute change in theoretical value between trade time and end-of-day after. Measures how much the option's
      fair value moved. Positive means TV increased.
    synonyms:
    - tv move eod
    formula: '(adjusted_tv_eod - swing_mid_tv)'
  - name: tv_change_buysell_eod
    type: FLOAT
    description: Directional TV change (buy/sell adjusted) at end-of-day, representing the realized PnL per unit from the
      TV movement. Positive = profitable direction.
    synonyms:
    - pnl eod
    formula: '(adjusted_tv_eod - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_eod_per_unit
    type: FLOAT
    description: Roll slippage component per unit (per contract) at the end-of-day horizon, measuring how much of the per-contract
      PnL change from trade time to end-of-day after was attributable to cost-of-carry (roll/dividend) change. Expressed in
      ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip eod
    - roll slip eod per unit
    formula: '((reference_roll_eod - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_eod
    type: FLOAT
    description: Roll slippage component (total, in currency) at the end-of-day horizon, measuring the total roll-driven PnL
      change from trade time to end-of-day after. Equals roll_slippage_eod_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip eod
    formula: '((reference_roll_eod - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_eod
    type: FLOAT
    description: Adjusted theoretical value at end-of-day after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_eod + delta_eod * (calculated_base_val_eod - (under_eod - roll_eod)) + 0.5 * gamma_eod * POW((calculated_base_val_eod - (under_eod - roll_eod)), 2)'
  - name: delta_slippage_eod_per_unit
    type: FLOAT
    description: Delta slippage component per unit (per contract) at the end-of-day horizon, measuring how much of the per-contract
      PnL change from trade time to end-of-day after was attributable to delta (underlying price movement). Expressed in ticks.
      Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip eod
    - delta slip eod per unit
    formula: '((calculated_base_val_eod - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_eod
    type: FLOAT
    description: Delta slippage component (total, in currency) at the end-of-day horizon, measuring the total delta-driven
      PnL change from trade time to end-of-day after. Equals delta_slippage_eod_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip eod
    formula: '((calculated_base_val_eod - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_eod_fired_at
    type: FLOAT
    description: 'Volatility slippage component at the end-of-day horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the volatility change contribution to PnL between trade time and end-of-day after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip eod
    - fire vol slip eod
    - fire vol pnl eod
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_eod for fill-based version.
    formula: '((vol_eod - vol) * ((vega_eod + vega) / 2) - vol_path_estimate_eod) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_eod_fired_at
    type: FLOAT
    description: 'Residual/other slippage component at the end-of-day horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the residual factors contribution to PnL between trade time and end-of-day after, based on
      the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip eod_fired_at
    - fire other slip eod
    - fire other pnl eod
    - fire unexplained pnl eod
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_eod for fill-based version.
    formula: '(tv_change_buysell_eod * contract_size * routed_size_capped) - (delta_slippage_eod_fired_at + roll_slippage_eod_fired_at)'
  - name: delta_slippage_eod_fired_at
    type: FLOAT
    description: 'Delta slippage component at the end-of-day horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the delta movement contribution to PnL between trade time and end-of-day after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip eod
    - fire delta slip eod
    - fire delta pnl eod
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_eod for fill-based version.
    formula: '((calculated_base_val_eod - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_eod_fired_at
    type: FLOAT
    description: 'Roll slippage component at the end-of-day horizon, computed using routed_size (fired size) instead of trade_volume.
      Measures the cost-of-carry change contribution to PnL between trade time and end-of-day after, based on the fired quantity.
      Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip eod
    - fire roll slip eod
    - fire roll pnl eod
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_eod for fill-based version.
    formula: '((reference_roll_eod - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_1D
    type: STRING
    description: Estimated volatility path effect at the 1 day (next open) horizon, measuring the expected impact of implied
      volatility mean-reversion or trending on option value between trade time and 1 day (next open) after.
    synonyms: []
    formula: '((calculated_base_val_1D - swing_mid_base_val) * ((delta_1D - raw_delta_mid_bv_1D) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_1D_per_unit
    type: STRING
    description: Volatility slippage component per unit (per contract) at the 1 day (next open) horizon, measuring how much
      of the per-contract PnL change from trade time to 1 day (next open) after was attributable to implied volatility change.
      Expressed in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 1d
    - vol slip 1D per unit
    formula: '((vol_1D - vol) * ((vega_1D + vega) / 2) - vol_path_estimate_1D) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1D
    type: STRING
    description: Volatility slippage component (total, in currency) at the 1 day (next open) horizon, measuring the total
      vol-driven PnL change from trade time to 1 day (next open) after. Equals vol_slippage_1D_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 1d
    formula: '((vol_1D - vol) * ((vega_1D + vega) / 2) - vol_path_estimate_1D) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_1D_per_unit
    type: STRING
    description: Residual/other slippage component per unit (per contract) at the 1 day (next open) horizon, measuring how
      much of the per-contract PnL change from trade time to 1 day (next open) after was attributable to residual factors
      not captured by delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other
      slippage in currency.
    synonyms:
    - other slip 1d
    - other slip 1D per unit
    formula: '(tv_change_buysell_1D * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_1D_per_unit + roll_slippage_1D_per_unit)'
  - name: other_slippage_1D
    type: STRING
    description: Residual/other slippage component (total, in currency) at the 1 day (next open) horizon, measuring the total
      other-driven PnL change from trade time to 1 day (next open) after. Equals other_slippage_1D_per_unit * trade_volume
      * contract_size.
    synonyms:
    - other slip 1d
    formula: '(tv_change_buysell_1D * contract_size * trade_volume) - (delta_slippage_1D + roll_slippage_1D)'
  - name: tv_1D
    type: STRING
    description: Theoretical value (fair value) at 1 day (next open) after the OTO swing trade, taken from the forward-looking
      theo data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 1d
    - theo 1D
  - name: delta_1D
    type: STRING
    description: Option delta at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 1d
    - delta 1D
  - name: gamma_1D
    type: STRING
    description: Option gamma at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_1D
    type: STRING
    description: Option vega at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_1D
    type: STRING
    description: Option theta at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_1D
    type: STRING
    description: Underlying price at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 1d
    - spot 1D
  - name: vol_1D
    type: STRING
    description: Implied volatility at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 1d
    - IV 1D
  - name: rho_1D
    type: STRING
    description: Option rho at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_1D
    type: STRING
    description: Raw (unadjusted) delta at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_1D
    type: STRING
    description: Raw (unadjusted) gamma at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_1D
    type: STRING
    description: Normalized strike (moneyness) at 1 day (next open) after the OTO swing trade, taken from the forward-looking
      theo data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_1D
    type: STRING
    description: Roll adjustment value at 1 day (next open) after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_1D
    type: STRING
    description: Absolute change in theoretical value between trade time and 1 day (next open) after. Measures how much the
      option's fair value moved. Positive means TV increased.
    synonyms:
    - tv move 1d
    formula: '(adjusted_tv_1D - swing_mid_tv)'
  - name: tv_change_buysell_1D
    type: STRING
    description: Directional TV change (buy/sell adjusted) at 1 day (next open), representing the realized PnL per unit from
      the TV movement. Positive = profitable direction.
    synonyms:
    - pnl 1d
    formula: '(adjusted_tv_1D - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_1D_per_unit
    type: STRING
    description: Roll slippage component per unit (per contract) at the 1 day (next open) horizon, measuring how much of the
      per-contract PnL change from trade time to 1 day (next open) after was attributable to cost-of-carry (roll/dividend)
      change. Expressed in ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 1d
    - roll slip 1D per unit
    formula: '((reference_roll_1D - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1D
    type: STRING
    description: Roll slippage component (total, in currency) at the 1 day (next open) horizon, measuring the total roll-driven
      PnL change from trade time to 1 day (next open) after. Equals roll_slippage_1D_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 1d
    formula: '((reference_roll_1D - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_1D
    type: STRING
    description: Adjusted theoretical value at 1 day (next open) after the OTO swing trade, taken from the forward-looking
      theo data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_1D + delta_1D * (calculated_base_val_1D - (under_1D - roll_1D)) + 0.5 * gamma_1D * POW((calculated_base_val_1D - (under_1D - roll_1D)), 2)'
  - name: delta_slippage_1D_per_unit
    type: STRING
    description: Delta slippage component per unit (per contract) at the 1 day (next open) horizon, measuring how much of
      the per-contract PnL change from trade time to 1 day (next open) after was attributable to delta (underlying price movement).
      Expressed in ticks. Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 1d
    - delta slip 1D per unit
    formula: '((calculated_base_val_1D - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_1D
    type: STRING
    description: Delta slippage component (total, in currency) at the 1 day (next open) horizon, measuring the total delta-driven
      PnL change from trade time to 1 day (next open) after. Equals delta_slippage_1D_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 1d
    formula: '((calculated_base_val_1D - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_1D_fired_at
    type: STRING
    description: 'Volatility slippage component at the 1 day (next open) horizon, computed using routed_size (fired size)
      instead of trade_volume. Measures the volatility change contribution to PnL between trade time and 1 day (next open)
      after, based on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 1d
    - fire vol slip 1D
    - fire vol pnl 1D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_1D for fill-based version.
    formula: '((vol_1D - vol) * ((vega_1D + vega) / 2) - vol_path_estimate_1D) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_1D_fired_at
    type: STRING
    description: 'Residual/other slippage component at the 1 day (next open) horizon, computed using routed_size (fired size)
      instead of trade_volume. Measures the residual factors contribution to PnL between trade time and 1 day (next open)
      after, based on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 1d_fired_at
    - fire other slip 1D
    - fire other pnl 1D
    - fire unexplained pnl 1D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_1D for fill-based version.
    formula: '(tv_change_buysell_1D * contract_size * routed_size_capped) - (delta_slippage_1D_fired_at + roll_slippage_1D_fired_at)'
  - name: delta_slippage_1D_fired_at
    type: STRING
    description: 'Delta slippage component at the 1 day (next open) horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the delta movement contribution to PnL between trade time and 1 day (next open) after, based
      on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 1d
    - fire delta slip 1D
    - fire delta pnl 1D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_1D for fill-based version.
    formula: '((calculated_base_val_1D - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_1D_fired_at
    type: STRING
    description: 'Roll slippage component at the 1 day (next open) horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the cost-of-carry change contribution to PnL between trade time and 1 day (next open) after,
      based on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 1d
    - fire roll slip 1D
    - fire roll pnl 1D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_1D for fill-based version.
    formula: '((reference_roll_1D - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: vol_path_estimate_5D
    type: STRING
    description: Estimated volatility path effect at the 5 days (T+5 open) horizon, measuring the expected impact of implied
      volatility mean-reversion or trending on option value between trade time and 5 days (T+5 open) after.
    synonyms: []
    formula: '((calculated_base_val_5D - swing_mid_base_val) * ((delta_5D - raw_delta_mid_bv_5D) + (delta - raw_delta_mid_bv)) / 2)'
  - name: vol_slippage_5D_per_unit
    type: STRING
    description: Volatility slippage component per unit (per contract) at the 5 days (T+5 open) horizon, measuring how much
      of the per-contract PnL change from trade time to 5 days (T+5 open) after was attributable to implied volatility change.
      Expressed in ticks. Multiply by trade_volume and contract_size for total vol slippage in currency.
    synonyms:
    - vol slip 5d
    - vol slip 5D per unit
    formula: '((vol_5D - vol) * ((vega_5D + vega) / 2) - vol_path_estimate_5D) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: vol_slippage_5D
    type: STRING
    description: Volatility slippage component (total, in currency) at the 5 days (T+5 open) horizon, measuring the total
      vol-driven PnL change from trade time to 5 days (T+5 open) after. Equals vol_slippage_5D_per_unit * trade_volume * contract_size.
    synonyms:
    - vol slip 5d
    formula: '((vol_5D - vol) * ((vega_5D + vega) / 2) - vol_path_estimate_5D) * trade_volume * contract_size * buy_sell_multiplier'
  - name: other_slippage_5D_per_unit
    type: STRING
    description: Residual/other slippage component per unit (per contract) at the 5 days (T+5 open) horizon, measuring how
      much of the per-contract PnL change from trade time to 5 days (T+5 open) after was attributable to residual factors
      not captured by delta, vol, or roll. Expressed in ticks. Multiply by trade_volume and contract_size for total other
      slippage in currency.
    synonyms:
    - other slip 5d
    - other slip 5D per unit
    formula: '(tv_change_buysell_5D * COALESCE(ABS(leg_ratio), 1) * contract_size) - (delta_slippage_5D_per_unit + roll_slippage_5D_per_unit)'
  - name: other_slippage_5D
    type: STRING
    description: Residual/other slippage component (total, in currency) at the 5 days (T+5 open) horizon, measuring the total
      other-driven PnL change from trade time to 5 days (T+5 open) after. Equals other_slippage_5D_per_unit * trade_volume
      * contract_size.
    synonyms:
    - other slip 5d
    formula: '(tv_change_buysell_5D * contract_size * trade_volume) - (delta_slippage_5D + roll_slippage_5D)'
  - name: tv_5D
    type: STRING
    description: Theoretical value (fair value) at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking
      theo data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - theo 5d
    - theo 5D
  - name: delta_5D
    type: STRING
    description: Option delta at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - delta 5d
    - delta 5D
  - name: gamma_5D
    type: STRING
    description: Option gamma at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: vega_5D
    type: STRING
    description: Option vega at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: theta_5D
    type: STRING
    description: Option theta at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: under_5D
    type: STRING
    description: Underlying price at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - spot 5d
    - spot 5D
  - name: vol_5D
    type: STRING
    description: Implied volatility at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo data
      snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms:
    - IV 5d
    - IV 5D
  - name: rho_5D
    type: STRING
    description: Option rho at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo data snapshot.
      Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_delta_5D
    type: STRING
    description: Raw (unadjusted) delta at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: raw_gamma_5D
    type: STRING
    description: Raw (unadjusted) gamma at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: norm_strike_5D
    type: STRING
    description: Normalized strike (moneyness) at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking
      theo data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: roll_5D
    type: STRING
    description: Roll adjustment value at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking theo
      data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
  - name: tv_change_5D
    type: STRING
    description: Absolute change in theoretical value between trade time and 5 days (T+5 open) after. Measures how much the
      option's fair value moved. Positive means TV increased.
    synonyms:
    - tv move 5d
    formula: '(adjusted_tv_5D - swing_mid_tv)'
  - name: tv_change_buysell_5D
    type: STRING
    description: Directional TV change (buy/sell adjusted) at 5 days (T+5 open), representing the realized PnL per unit from
      the TV movement. Positive = profitable direction.
    synonyms:
    - pnl 5d
    formula: '(adjusted_tv_5D - swing_mid_tv) * buy_sell_multiplier'
  - name: roll_slippage_5D_per_unit
    type: STRING
    description: Roll slippage component per unit (per contract) at the 5 days (T+5 open) horizon, measuring how much of the
      per-contract PnL change from trade time to 5 days (T+5 open) after was attributable to cost-of-carry (roll/dividend)
      change. Expressed in ticks. Multiply by trade_volume and contract_size for total roll slippage in currency.
    synonyms:
    - roll slip 5d
    - roll slip 5D per unit
    formula: '((reference_roll_5D - reference_roll) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: roll_slippage_5D
    type: STRING
    description: Roll slippage component (total, in currency) at the 5 days (T+5 open) horizon, measuring the total roll-driven
      PnL change from trade time to 5 days (T+5 open) after. Equals roll_slippage_5D_per_unit * trade_volume * contract_size.
    synonyms:
    - roll slip 5d
    formula: '((reference_roll_5D - reference_roll) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: adjusted_tv_5D
    type: STRING
    description: Adjusted theoretical value at 5 days (T+5 open) after the OTO swing trade, taken from the forward-looking
      theo data snapshot. Used to compute slippage and track how the option's characteristics evolved after the trade.
    synonyms: []
    formula: 'tv_5D + delta_5D * (calculated_base_val_5D - (under_5D - roll_5D)) + 0.5 * gamma_5D * POW((calculated_base_val_5D - (under_5D - roll_5D)), 2)'
  - name: delta_slippage_5D_per_unit
    type: STRING
    description: Delta slippage component per unit (per contract) at the 5 days (T+5 open) horizon, measuring how much of
      the per-contract PnL change from trade time to 5 days (T+5 open) after was attributable to delta (underlying price movement).
      Expressed in ticks. Multiply by trade_volume and contract_size for total delta slippage in currency.
    synonyms:
    - delta slip 5d
    - delta slip 5D per unit
    formula: '((calculated_base_val_5D - swing_mid_base_val) * delta) * COALESCE(ABS(leg_ratio), 1) * contract_size * buy_sell_multiplier'
  - name: delta_slippage_5D
    type: STRING
    description: Delta slippage component (total, in currency) at the 5 days (T+5 open) horizon, measuring the total delta-driven
      PnL change from trade time to 5 days (T+5 open) after. Equals delta_slippage_5D_per_unit * trade_volume * contract_size.
    synonyms:
    - delta slip 5d
    formula: '((calculated_base_val_5D - swing_mid_base_val) * delta) * trade_volume * contract_size * buy_sell_multiplier'
  - name: vol_slippage_5D_fired_at
    type: STRING
    description: 'Volatility slippage component at the 5 days (T+5 open) horizon, computed using routed_size (fired size)
      instead of trade_volume. Measures the volatility change contribution to PnL between trade time and 5 days (T+5 open)
      after, based on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - vol slip 5d
    - fire vol slip 5D
    - fire vol pnl 5D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with vol_slippage_5D for fill-based version.
    formula: '((vol_5D - vol) * ((vega_5D + vega) / 2) - vol_path_estimate_5D) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: other_slippage_5D_fired_at
    type: STRING
    description: 'Residual/other slippage component at the 5 days (T+5 open) horizon, computed using routed_size (fired size)
      instead of trade_volume. Measures the residual factors contribution to PnL between trade time and 5 days (T+5 open)
      after, based on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - other slip 5d_fired_at
    - fire other slip 5D
    - fire other pnl 5D
    - fire unexplained pnl 5D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with other_slippage_5D for fill-based version.
    formula: '(tv_change_buysell_5D * contract_size * routed_size_capped) - (delta_slippage_5D_fired_at + roll_slippage_5D_fired_at)'
  - name: delta_slippage_5D_fired_at
    type: STRING
    description: 'Delta slippage component at the 5 days (T+5 open) horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the delta movement contribution to PnL between trade time and 5 days (T+5 open) after, based
      on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - delta slip 5d
    - fire delta slip 5D
    - fire delta pnl 5D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with delta_slippage_5D for fill-based version.
    formula: '((calculated_base_val_5D - swing_mid_base_val) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: roll_slippage_5D_fired_at
    type: STRING
    description: 'Roll slippage component at the 5 days (T+5 open) horizon, computed using routed_size (fired size) instead
      of trade_volume. Measures the cost-of-carry change contribution to PnL between trade time and 5 days (T+5 open) after,
      based on the fired quantity. Unique to otoswing: fired_at variants measure PnL relative to the routing decision.'
    synonyms:
    - roll slip 5d
    - fire roll slip 5D
    - fire roll pnl 5D
    source: kpi_otoswing_calculations.sql (KPI-Added, fired_at variant)
    business_rules: Uses routed_size, not trade_volume. Compare with roll_slippage_5D for fill-based version.
    formula: '((reference_roll_5D - reference_roll) * delta) * routed_size_capped * contract_size * buy_sell_multiplier'
  - name: calculated_base_val_eod
    type: FLOAT
    description: Calculated base (underlying) value at the end-of-day forward snapshot.
    synonyms: []
  - name: calculated_base_val_1D
    type: STRING
    description: Calculated base (underlying) value at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: calculated_base_val_5D
    type: STRING
    description: Calculated base (underlying) value at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: delta
    type: FLOAT
    description: Option delta at the time of the OTO swing trade, measuring the sensitivity of the option's price to a one-unit
      move in the underlying. Ranges from -1 to +1 for vanilla options. Used for hedging calculations and as a moneyness proxy.
      Also known as delta exposure or hedge ratio.
    synonyms:
    - delta exposure
    - hedge ratio
    - option delta
    source: kpi_otoswing_normalized.sql
    formula: 'ref_delta + (delta_adjusted_base_price - ref_base_price) * ref_gamma'
  - name: gamma
    type: FLOAT
    description: Option gamma at the time of the OTO swing trade, measuring the rate of change of delta with respect to the
      underlying price. Higher gamma means delta changes more rapidly. Important for understanding second-order hedging risk
      and the curvature of the PnL profile.
    synonyms:
    - gamma exposure
    - convexity
    - option gamma
  - name: vega
    type: FLOAT
    description: Option vega at the time of the OTO swing trade, measuring sensitivity to a one-point change in implied volatility.
      Higher vega means greater exposure to volatility changes. Used in vol_slippage decomposition.
    synonyms:
    - vol sensitivity
    - vega exposure
    - option vega
  - name: theta
    type: FLOAT
    description: Option theta at the time of the OTO swing trade, measuring time decay per day. Represents the theoretical
      loss in option value from one day passing with all else equal. Always negative for long options.
    synonyms:
    - time decay
    - theta decay
    - option theta
  - name: under
    type: FLOAT
    description: Underlying instrument price at the time of the OTO swing trade. This is the spot or futures price of the
      asset underlying the option. Used as a reference for delta hedging and moneyness calculations.
    synonyms:
    - underlying
    - spot
    - underlying price
    - spot price
  - name: vol
    type: FLOAT
    description: Implied volatility of the option at the time of the OTO swing trade, expressed as an annualized percentage.
      Represents the market's expectation of future price variability. Used in vol_slippage decomposition and for moneyness
      analysis.
    synonyms:
    - implied volatility
    - IV
    - sigma
    - implied vol
    - volatility
  - name: rho
    type: FLOAT
    description: Option rho at the time of the OTO swing trade, measuring sensitivity to a change in the risk-free interest
      rate. Generally small for short-dated options but can be material for longer-dated contracts.
    synonyms:
    - rate sensitivity
    - interest rate sensitivity
  - name: raw_delta
    type: FLOAT
    description: Raw (unadjusted) option delta before any proprietary modifications or smoothing. The model-output delta before
      the system applies its adjustments. Compare with the adjusted delta field for the production-used value.
    synonyms:
    - unadjusted delta
    - model delta
  - name: raw_gamma
    type: FLOAT
    description: Raw (unadjusted) option gamma before any proprietary modifications or smoothing. The model-output gamma before
      the system applies its adjustments.
    synonyms:
    - unadjusted gamma
    - model gamma
  - name: norm_strike
    type: FLOAT
    description: Normalized strike measuring moneyness of the option, typically expressed as a ratio or difference relative
      to the underlying price. Used to bucket trades by moneyness (ATM, OTM, ITM). Traders often call this delta bucket or
      moneyness.
    synonyms:
    - moneyness
    - delta bucket
    - normalized strike
    - strike distance
  - name: roll
    type: FLOAT
    description: Roll adjustment value at the time of the OTO swing trade, representing the cost-of-carry difference between
      the forward and spot prices. Used in roll_slippage decomposition.
    synonyms:
    - calendar roll
    - roll value
    - carry adjustment
  - name: abs_trade_volume
    type: FLOAT
    description: Absolute (unsigned) trade volume for this OTO swing, always positive regardless of buy/sell direction. Use
      this for aggregations where direction does not matter, such as total volume traded.
    synonyms:
    - unsigned size
    - absolute lots
    - abs volume
    business_rules: Always positive. Use for SUM/AVG when you want total volume.
  - name: abs_routed_size
    type: FLOAT
    description: Absolute (unsigned) routed size for this OTO swing trade. The unsigned version of routed_size, useful for
      aggregation and averaging where direction does not matter. Always positive regardless of buy/sell direction.
    synonyms:
    - unsigned size
    - absolute lots
    - unsigned routed size
    - absolute fire size
    - abs fire size
    business_rules: Use this for SUM/AVG aggregations where you want total volume regardless of direction.
    source: kpi_otoswing_calculations.sql
  - name: tte_1s
    type: FLOAT
    description: Time to expiry at the 1 second forward snapshot, in years.
    synonyms: []
  - name: forward_1s
    type: FLOAT
    description: Forward price at the 1 second forward snapshot.
    synonyms: []
  - name: short_rate_1s
    type: FLOAT
    description: Short-term interest rate at the 1 second forward snapshot.
    synonyms: []
  - name: stock_roll_1s
    type: FLOAT
    description: Stock roll value at the 1 second forward snapshot.
    synonyms: []
  - name: reference_roll_1s
    type: FLOAT
    description: Reference roll value at the 1 second forward snapshot.
    synonyms: []
  - name: tte_5s
    type: FLOAT
    description: Time to expiry at the 5 seconds forward snapshot, in years.
    synonyms: []
  - name: forward_5s
    type: FLOAT
    description: Forward price at the 5 seconds forward snapshot.
    synonyms: []
  - name: short_rate_5s
    type: FLOAT
    description: Short-term interest rate at the 5 seconds forward snapshot.
    synonyms: []
  - name: stock_roll_5s
    type: FLOAT
    description: Stock roll value at the 5 seconds forward snapshot.
    synonyms: []
  - name: reference_roll_5s
    type: FLOAT
    description: Reference roll value at the 5 seconds forward snapshot.
    synonyms: []
  - name: tte_10s
    type: FLOAT
    description: Time to expiry at the 10 seconds forward snapshot, in years.
    synonyms: []
  - name: forward_10s
    type: FLOAT
    description: Forward price at the 10 seconds forward snapshot.
    synonyms: []
  - name: short_rate_10s
    type: FLOAT
    description: Short-term interest rate at the 10 seconds forward snapshot.
    synonyms: []
  - name: stock_roll_10s
    type: FLOAT
    description: Stock roll value at the 10 seconds forward snapshot.
    synonyms: []
  - name: reference_roll_10s
    type: FLOAT
    description: Reference roll value at the 10 seconds forward snapshot.
    synonyms: []
  - name: tte_30s
    type: FLOAT
    description: Time to expiry at the 30 seconds forward snapshot, in years.
    synonyms: []
  - name: forward_30s
    type: FLOAT
    description: Forward price at the 30 seconds forward snapshot.
    synonyms: []
  - name: short_rate_30s
    type: FLOAT
    description: Short-term interest rate at the 30 seconds forward snapshot.
    synonyms: []
  - name: stock_roll_30s
    type: FLOAT
    description: Stock roll value at the 30 seconds forward snapshot.
    synonyms: []
  - name: reference_roll_30s
    type: FLOAT
    description: Reference roll value at the 30 seconds forward snapshot.
    synonyms: []
  - name: tte_1m
    type: FLOAT
    description: Time to expiry at the 1 minute forward snapshot, in years.
    synonyms: []
  - name: forward_1m
    type: FLOAT
    description: Forward price at the 1 minute forward snapshot.
    synonyms: []
  - name: short_rate_1m
    type: FLOAT
    description: Short-term interest rate at the 1 minute forward snapshot.
    synonyms: []
  - name: stock_roll_1m
    type: FLOAT
    description: Stock roll value at the 1 minute forward snapshot.
    synonyms: []
  - name: reference_roll_1m
    type: FLOAT
    description: Reference roll value at the 1 minute forward snapshot.
    synonyms: []
  - name: tte_5m
    type: FLOAT
    description: Time to expiry at the 5 minutes forward snapshot, in years.
    synonyms: []
  - name: forward_5m
    type: FLOAT
    description: Forward price at the 5 minutes forward snapshot.
    synonyms: []
  - name: short_rate_5m
    type: FLOAT
    description: Short-term interest rate at the 5 minutes forward snapshot.
    synonyms: []
  - name: stock_roll_5m
    type: FLOAT
    description: Stock roll value at the 5 minutes forward snapshot.
    synonyms: []
  - name: reference_roll_5m
    type: FLOAT
    description: Reference roll value at the 5 minutes forward snapshot.
    synonyms: []
  - name: tte_10m
    type: FLOAT
    description: Time to expiry at the 10 minutes forward snapshot, in years.
    synonyms: []
  - name: forward_10m
    type: FLOAT
    description: Forward price at the 10 minutes forward snapshot.
    synonyms: []
  - name: short_rate_10m
    type: FLOAT
    description: Short-term interest rate at the 10 minutes forward snapshot.
    synonyms: []
  - name: stock_roll_10m
    type: FLOAT
    description: Stock roll value at the 10 minutes forward snapshot.
    synonyms: []
  - name: reference_roll_10m
    type: FLOAT
    description: Reference roll value at the 10 minutes forward snapshot.
    synonyms: []
  - name: tte_30m
    type: FLOAT
    description: Time to expiry at the 30 minutes forward snapshot, in years.
    synonyms: []
  - name: forward_30m
    type: FLOAT
    description: Forward price at the 30 minutes forward snapshot.
    synonyms: []
  - name: short_rate_30m
    type: FLOAT
    description: Short-term interest rate at the 30 minutes forward snapshot.
    synonyms: []
  - name: stock_roll_30m
    type: FLOAT
    description: Stock roll value at the 30 minutes forward snapshot.
    synonyms: []
  - name: reference_roll_30m
    type: FLOAT
    description: Reference roll value at the 30 minutes forward snapshot.
    synonyms: []
  - name: tte_1h
    type: FLOAT
    description: Time to expiry at the 1 hour forward snapshot, in years.
    synonyms: []
  - name: forward_1h
    type: FLOAT
    description: Forward price at the 1 hour forward snapshot.
    synonyms: []
  - name: short_rate_1h
    type: FLOAT
    description: Short-term interest rate at the 1 hour forward snapshot.
    synonyms: []
  - name: stock_roll_1h
    type: FLOAT
    description: Stock roll value at the 1 hour forward snapshot.
    synonyms: []
  - name: reference_roll_1h
    type: FLOAT
    description: Reference roll value at the 1 hour forward snapshot.
    synonyms: []
  - name: tte_eod
    type: FLOAT
    description: Time to expiry at the end-of-day forward snapshot, in years.
    synonyms: []
  - name: forward_eod
    type: FLOAT
    description: Forward price at the end-of-day forward snapshot.
    synonyms: []
  - name: short_rate_eod
    type: FLOAT
    description: Short-term interest rate at the end-of-day forward snapshot.
    synonyms: []
  - name: stock_roll_eod
    type: FLOAT
    description: Stock roll value at the end-of-day forward snapshot.
    synonyms: []
  - name: reference_roll_eod
    type: FLOAT
    description: Reference roll value at the end-of-day forward snapshot.
    synonyms: []
  - name: tte_1D
    type: STRING
    description: Time to expiry at the 1 day (next open) forward snapshot, in years.
    synonyms: []
  - name: forward_1D
    type: STRING
    description: Forward price at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: short_rate_1D
    type: STRING
    description: Short-term interest rate at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: stock_roll_1D
    type: STRING
    description: Stock roll value at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: reference_roll_1D
    type: STRING
    description: Reference roll value at the 1 day (next open) forward snapshot.
    synonyms: []
  - name: tte_5D
    type: STRING
    description: Time to expiry at the 5 days (T+5 open) forward snapshot, in years.
    synonyms: []
  - name: forward_5D
    type: STRING
    description: Forward price at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: short_rate_5D
    type: STRING
    description: Short-term interest rate at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: stock_roll_5D
    type: STRING
    description: Stock roll value at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: reference_roll_5D
    type: STRING
    description: Reference roll value at the 5 days (T+5 open) forward snapshot.
    synonyms: []
  - name: tte
    type: FLOAT
    description: Time to expiry of the option in years (annualized) at the time of the OTO swing trade. A key input to option
      pricing models. Larger values indicate longer-dated options with more time value. Also known as DTE (days to expiry)
      when expressed in days.
    synonyms:
    - time to expiry
    - days to expiry
    - DTE
    - time to maturity
  - name: forward
    type: FLOAT
    description: Forward price of the underlying instrument at the time of the OTO swing trade. Accounts for cost of carry
      (dividends, interest) between spot and expiry. Used as the underlying price input in Black-Scholes style pricing.
    synonyms:
    - forward price
    - fwd
    - fwd price
  - name: short_rate
    type: FLOAT
    description: Short-term risk-free interest rate used in option pricing at the time of the OTO swing trade. Affects forward
      pricing and option time value.
    synonyms:
    - rate
    - risk-free rate
    - interest rate
  - name: stock_roll
    type: FLOAT
    description: Stock roll value at the time of the OTO swing trade, representing the roll cost associated with the underlying
      equity position.
    synonyms: []
    formula: 'forward - under'
  - name: reference_roll
    type: FLOAT
    description: Reference roll value, the roll figure captured at the reference snapshot used for KPI computation baseline.
    synonyms: []
    formula: 'CASE WHEN parent_is_combo = TRUE THEN ((forward - under) + roll) ELSE ((forward - under) + ref_roll) END'
  - name: is_expiry_trade
    type: BOOLEAN
    description: 'Boolean indicating whether this OTO swing trade occurred on the option''s expiry day. Expiry-day trades
      have unique characteristics: gamma explosion, pin risk, and different liquidity dynamics.'
    synonyms:
    - expiry trade
    - on expiry
    business_rules: Expiry-day trades often have extreme greeks. Consider analyzing separately.
  note: OTO sweep trades. Has unique fired_at metrics that measure PnL relative to the routed/fired price, separate from filled/average
    price. When a trader asks 'oto sweep performance', 'how much did we swing', or 'swing edge', use this table.
